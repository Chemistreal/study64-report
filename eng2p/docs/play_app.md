# 판 화면

신뢰도: A 생성 (설계)
상위 규격: docs/play_rules.md / docs/round.md 8장 / docs/roadmap.md 12.14
작성일: 2026-08-09 (T259). 첫 판을 붙이며 공통 틀을 같이 정했다

검증로그: 2026-08-09 / 거울 판을 붙이고 두 화면을 나란히 열어 답이 한쪽에만 있는지 봤다 / 통과 / 기기 쪽을 안 고른 날은 이 판을 안 돌리기로 했다
검증로그: 2026-08-09 / check_play_screen.js 로 재고 열 자리를 깨서 다 잡히는지 봤다 (T260) / 통과 / 실패를 모아 두던 것을 찾은 자리에서 적게 고쳤다

## 1. 이 문서가 무엇인가

`docs/play_rules.md` 는 판마다 아홉 줄로 **무엇을 하는가**를 적었다.
`docs/round.md` 8장은 두 화면을 가르는 **조각**을 만들어 뒀다.
그 둘 사이가 비어 있다. 규칙서의 아홉 줄이 화면의 어느 자리가 되는가.

**여기서 한 번 정하고 스무 번 다시 안 정한다.**
스무 번 다시 정하면 스무 벌이 조금씩 달라지고 그중 하나가 안 가린다.

## 2. 아홉 줄이 화면의 어디로 가는가

| 규칙서 칸 | 화면의 어디 |
|---|---|
| 트랙 구조 분 | 판 목록의 단추에 적는다. 고르기 전에 보인다 |
| 쓰는 것 | 판을 열 때 게으르게 읽는다. **등급을 화면에 적는다** |
| 시작 조건 | 못 갖췄으면 판을 안 돌리고 **왜 안 도는지**를 적는다 |
| 역할 | `roundRole` 이 자리 이름을 낸다. 바뀔 때 `turnAlert` 이 알린다 |
| 도는 차례 | 자리마다 다른 화면. 한쪽에만 있는 것은 **그리지 않는다** |
| 판정 | 규칙서가 정한 사람의 기기에만 판정 단추가 있다 |
| 끝 조건 | 다 돌면 마감 화면. 분 제한은 시계가 알린다 |
| 못 했을 때 | 그 자리에 적는다. **벌이 없다는 것도 같이 적는다** |
| 기록할 값 | 그날 셈으로 남긴다. 사람별 칸을 안 만든다 |

## 3. 한쪽에만 있는 것은 그리지 않는다

`docs/round.md` 8.1 이 가리기를 만들었다. 자리를 남기고 왜 안 보이는지를 적는 것이다.
**그것으로 안 되는 자리가 있다.** 정답이다.

가린 것은 화면에 있다. 없는 것이 아니라 안 보이는 것이다.
T244 에서 화면에 남은 글자를 세어 가린 것을 쟀는데, 그 말은 **글자가 거기 있다**는 뜻이다.
CSS 한 줄이 어긋나면 보인다. 실제로 T244 에 `.veilcover` 가 `[hidden]` 을 이겼다.

그래서 정답은 이렇게 한다.

| | |
|---|---|
| 가리기 | 상대 몫. 있는데 안 보이는 것. `veilPane` |
| **안 그리기** | 정답. **이 기기의 화면에 아예 안 만든다** |

거울 판에서 짚는 쪽 화면에는 어느 쪽을 읽으라고 했는지가 한 글자도 없다.
`mirTarget` 을 짚는 쪽에서는 부르지 않는다. 부르면 값이 생기고
값이 생기면 언젠가 어딘가에 그려진다.

## 4. 기기 쪽을 안 골랐을 때

`docs/round.md` 8.1 은 기기 쪽을 안 골랐으면 **안 가린다**고 정했다.
한 기기로 도는 날이고 볼 사람이 하나라서다.

**정보 격차 판에서는 그러면 안 된다.** 짚는 쪽이 답을 보면
그 판은 아무것도 안 재는 판이 된다. 격차가 뼈대인데 격차가 없어진다.

| 판 갈래 | 기기 쪽을 안 골랐을 때 |
|---|---|
| 격차가 없는 판 | 둘 다 보인다. round.md 8.1 그대로 |
| **격차 판** | **안 돌린다.** 왜 안 도는지와 어떻게 고치는지를 적는다 |

둘 다 보이는 화면을 주고 "알아서 안 보세요" 라고 하는 것보다
**안 주고 왜 안 주는지 적는 것**이 낫다. 앞엣것은 두 사람이 그 판을 다 돌고
"이거 원래 이런 건가" 하고 넘어간다. 뒤엣것은 그 자리에서 고치게 만든다.

## 5. 판정을 누가 어느 기기에서 하는가

규칙서가 판마다 **누가** 판정하는지를 적어 뒀다. 원칙 3이다.
화면은 그 사람의 기기에만 판정 단추를 둔다.

그런데 여기에 이 과정의 구조가 하나 걸린다.
**자리가 판 안에서 바뀐다.** 거울은 넉 줄마다 바뀐다.
그러면 여덟 줄 중 넉 줄은 이 기기가 판정하고 넉 줄은 상대 기기가 판정한다.

**한 기기에 절반만 있다.** 규칙서가 남기라는 값은 "여덟 중 몇" 인데
어느 기기에도 여덟이 없다.

기기끼리 못 더한다. 망이 없다 (`docs/round.md` 2장).
그래서 여기서도 5장의 그 규칙을 쓴다. **기기가 못 하는 것을 사람이 한다.**

| 누가 | 무엇 |
|---|---|
| 기기 | 이 기기가 판정한 줄과 그중 건너간 줄을 센다 |
| **화면** | **이 숫자가 절반이라고 적는다** |
| 사람 | 두 숫자를 소리 내어 더한다 |

가운데 줄이 제일 중요하다. 안 적으면 두 사람이 이 숫자를 판의 셈으로 읽는다.
넷 중 셋을 보고 "여덟 중 셋" 이라고 적는다. 그것이 제일 나쁘다.

합치는 규칙은 T320 이 정한다. 그때까지 `rhit` 는 안 건너간다
(`docs/merge.md` 3.1).

## 6. 자료 등급을 화면이 말한다

CLAUDE.md 가 정한 1순위 규칙이 확신 없는 것을 B등급으로 표시하는 것이다.
**표시가 파일에만 있으면 두 사람은 그것을 못 본다.**

`out/data/pairs.json` 이 스스로 `"grade":"B"` 라고 적었다 (T258).
판 화면은 그 값을 그대로 옮긴다. 내가 다시 안 적는다.
자료가 A등급이 되는 날 화면도 같이 바뀌어야 하고, 옮겨 적으면 안 바뀐다.

B등급 자료로 도는 판은 **Q1 소리 트랙 통과 판정에 안 쓴다.**
C-gen 음성을 통과 판정에 안 쓰는 것과 같은 자리다. 연습에는 쓴다.

## 7. 거울 (T259)

규칙서 3.1 이다.

### 7.1 여덟을 대립을 섞어 만든다

T258 이 자료를 만들며 이 판의 꼴을 바꿨다.
**한 대립에서 여덟이 안 나온다.** 대본에서 제일 많이 나온 것이 여섯이고
l/r 은 하나뿐이고 b/v 는 없다.

그래서 열다섯 쌍을 한 자루에 담고 씨앗대로 섞어 여덟을 뽑는다.

**섞는 것이 오히려 낫다.** 한 대립만 여덟 번 하면 셋째 줄부터는 안 듣고 짚는다.
무엇을 듣는지 아니까 반만 들어도 갈린다.
자료가 못 하게 한 것이 판을 더 낫게 만들었다.

### 7.2 자리마다 무엇이 뜨는가

| | 읽는 쪽 | 짚는 쪽 |
|---|---|---|
| 낱말 | **읽을 것 하나.** 크게 | 둘 다. 번호를 붙여 |
| 어느 쪽인가 | 이 화면에만 | **없다.** 그리지 않는다 |
| 단추 | 짚었다 / 못 짚었다 | 두 낱말. 고른 뒤에 다음 줄 |
| 판정 | **여기서 한다** | 못 한다. 읽은 사람이 말해 준다 |

짚는 쪽이 고르고 나면 화면이 **소리 내어 말하라**고 시킨다.
말 안 하면 읽는 쪽이 무엇을 짚었는지 모르고 판정을 못 한다.
`docs/round.md` 5장의 그 자리다. 시키지 않으면 사람도 안 한다.

### 7.3 못 짚었을 때

규칙서: 못 짚으면 읽는 쪽이 한 번 더 읽는다. 두 번째도 못 짚으면 넘어간다.

화면이 그대로 한다. 첫 번째 "못 짚었다" 는 같은 줄에 머물고
두 번째는 넘어간다. **넘어간 줄도 판정한 줄로 센다.** 건너간 줄로는 안 센다.

벌이 없다는 것을 화면이 적는다 (원칙 4).
누구 잘못인지 안 가른다. 읽기가 문제일 수도 듣기가 문제일 수도 있고 그 둘을 못 가른다.

### 7.4 기기가 하나인 날

`docs/solo_plays.md` 3장: 돌려 보기. **읽는 쪽이 든다. 넉 줄마다 건넨다.**

짚는 쪽은 기기를 안 만진다. 종이 쌍 표를 보고 손으로 짚거나 소리 내어 말한다
(같은 문서 7.3: 이 판은 종이로 돈다. 쌍 표만 있으면 된다).

넉 줄마다 건네면 `soloTake` 가 자리를 뒤집고 `soloFirst` 가 그것을 받아
**받은 사람이 읽는 쪽이 된다.** 자리 돌기와 건네기가 같은 방향으로 맞물린다.

### 7.5 4분 시계

**기기마다 따로 간다.** 두 기기가 각자 눌러 몇 초씩 어긋난다.
맞추려 들지 않는다. 맞추려면 망이 있어야 하고 망이 없다.

규칙서의 4분은 사람이 끝내는 선이고 시계는 그 선을 알려 주는 것뿐이다.
끝내는 것은 둘이 말해서 한다.

판 탭을 떠나면 시계를 멈춘다. 안 멈추면 안 보이는 자리에서 매초 돈다.
보이지도 않는 칸이 도는 것을 T185 에서 한 번 겪었다.

## 7.6 한 줄 바꾸기 (T262)

규칙서 3.2 다. 거울 판과 갈리는 자리가 둘이다.

| | 거울 | 한 줄 바꾸기 |
|---|---|---|
| 자리가 바뀌는 때 | 넉 줄마다 | **한 줄마다** |
| 상대가 보는 것 | 쌍 둘 (답은 없다) | **원문 한 줄** |

뒤엣것이 이 판의 뼈대다. 시작 조건이 "오늘 과의 대본이 열려 있다" 이다.
찾는 쪽은 눈으로 원문을 따라가며 귀로 다른 데를 찾는다.

그래서 **읽는 쪽 화면에만 있는 것은 바꾼 낱말 하나**다. 바뀐 줄 전체가 아니다.
`swpFinder` 는 `it` 을 아예 안 받는다. **안 받으면 못 그린다.**
3장이 "안 그린다" 고 한 것을 여기서는 함수의 인자로 지킨다.

### 못 찾았을 때가 거울과 다르다

거울은 한 번 더 읽고 두 번째에 넘어간다. 여기는 규칙서가
**"어디였는지 알려 주고 다시 읽는다"** 고 적었다.

알려 준 뒤에는 찾는 쪽이 이미 안다. **그래서 다시 판정하지 않는다.**
그 줄은 못 찾은 줄이고 알려 준 뒤에 다음 줄로 간다.
같은 "못 했을 때" 라도 판마다 다르게 적혀 있고 화면은 적힌 대로 한다.

### 다섯을 못 채우는 과가 있다

1과가 셋이다 (T261). 화면이 첫 줄에서 **몇 줄인지를 적고 있는 만큼 돈다.**
다섯인 척하지 않고 이웃 과에서 가져오지도 않는다.
시작 조건이 "오늘 과" 이고 다른 과의 줄은 그 조건 밖이다.

### 한 번 막은 글을 또 막지 않는다

읽는 쪽 화면은 바꿀 자리를 줄 안에서 짚어 준다. `esc` 를 거친 글에서
자리를 찾고, 찾은 조각은 **이미 막혀 있다.** 거기에 `esc` 를 또 걸면
`&amp;` 가 `&amp;amp;` 가 되어 화면에 그대로 뜬다.
대본에 `&` 나 `<` 가 든 줄이 여섯 개 있다. 그 여섯에서 이 자리가 났다.

## 7.7 내 소리는 네가 (T265)

규칙서 3.3 이다. 앞의 둘과 갈리는 자리가 셋이다.

| | 거울 · 한 줄 바꾸기 | 내 소리는 네가 |
|---|---|---|
| 판정 단추가 있는 곳 | 읽는 쪽 | **듣는 쪽** |
| 세는 것 | 짚은 줄 / 찾은 줄 | **짚을 것이 없었던 줄** |
| 기기가 하나일 때 | 돌려 보기 | **종이. 돌려 보기가 안 된다** |

### 판정이 자리를 따라간다

`docs/play.md` 원칙 3이 판마다 **누가** 판정하는지를 정했고 규칙서가 판마다 적었다.
거울과 한 줄 바꾸기는 읽은 사람이고 이 판은 **듣는 사람**이다.
"자기 발음은 자기가 못 듣는다" 가 그 이유다.

화면은 그 사람의 기기에만 단추를 둔다. `hrmBind` 가 `first` 면 바로 돌아간다.
**말하는 쪽 화면에는 누를 것이 없다.**

### 세는 것이 못 한 것이 아니다

규칙서가 남기라는 값이 "여섯 줄 중 **짚을 것이 없었던 줄**이 몇인가" 다.
깨끗한 줄을 센다. 틀린 줄을 세면 그 숫자가 곧 못한 셈이 되고
`docs/play.md` 원칙 4가 그것을 막는다.

**화면이 그것을 한 줄로 적는다.** 안 적으면 두 사람이 이 숫자를 거꾸로 읽는다.

### 기기가 하나여도 돈다. 다만 돌려 보기가 아니다

`docs/solo_plays.md` 4.1 이 정했다. 둘이 **같은 순간에** 다른 것을 봐야 한다.
돌려 보기는 한 사람이 보고 건네는 것이라 이 자리를 못 채운다.

대본은 종이 강의록에 있고 지시는 화면에 있다.
그래서 **기기를 든 사람이 늘 듣는 쪽**이고 건네지 않는다.

시작 조건도 다르다. "기기 수는 상관없다" 이다. 그래서 기기 쪽을 안 골라도
이 판은 **막지 않는다.** 앞의 둘은 막았다. 그 둘은 격차가 뼈대인데
이 판은 협력형이고 종이로도 돈다. **같은 조건에 같은 답을 안 준다.**

### 자료에 마크다운을 안 쓴다

`listen.js` 의 지시 글에 `**` 를 넣어 뒀다가 화면에 별표 두 개가 그대로 떴다.
이 글은 문서로 안 가고 화면으로 간다. 화면은 글자를 막아 그린다.
**쓸 자리를 보고 쓴다.**

## 7.8 전달 놀이 (T268)

규칙서 3.4 다. 앞의 셋과 갈리는 자리가 셋이다.

| | 앞의 셋 | 전달 놀이 |
|---|---|---|
| 소리를 누가 내나 | 사람 | **앱.** 스무 판 중 하나뿐이다 |
| 판정 | 사람 | **기계.** 적은 글과 원문을 견준다 |
| 되짚기 | 없음 | **어디서 틀어졌는지를 낱말로 짚는다** |

### 이 판만 이어폰을 묻는다

`docs/round.md` 13장이 T245 에 이 자리를 미리 만들어 뒀다.
스무 판을 훑어 앱이 소리를 내는데 한쪽만 들어야 하는 판이 하나라고 셌고
그것이 이 판이다. `earAsk` 를 만들어 두고 세 판을 지나 여기서 처음 쓴다.

소리는 화면처럼 못 가른다. 한 상에서 울리면 둘 다 듣는다.
앱이 할 수 있는 것은 **어느 기기가 소리를 낼지**뿐이고 나머지는 이어폰이다.
소리를 안 내는 기기에는 "이어폰은 상대 기기에 끼운다" 가 뜬다 (T254).

### 판정을 기계에 맡기는 자리

앞의 셋은 사람이 판정했다. 소리를 재는 일이라 기계가 못 한다.
여기서 재는 것은 **적은 글과 원문이 몇 군데 다른가**다. 그것은 셀 수 있다.

    낱말이 아니라 **자리**를 센다. 이어 붙은 것은 한 군데다
    대소문자와 문장부호는 안 본다. 말로 옮긴 것을 받아 적은 글이다

규칙서가 "어디서 틀어졌는지 되짚는다. **그것이 이 판의 목적이다**" 라고 적었다.
목적이라고 적힌 것을 사람 눈에 맡기면 안 한다. 기계가 짚어 준다.

### 소리 자리가 어림이다

`cues.js` 는 실측이 아니다 (`docs/audio_timing.md`). 조금 일찍 끊기거나
늦게 시작할 수 있다. **화면이 그렇다고 적고 다시 듣기를 준다.**
끝은 0.7초 넘겨 잡는다. 일찍 끊는 것보다 조금 더 듣는 것이 낫다.

### 적어도 펴기가 안 켜졌다

`revealGate` 는 **그릴 때** 칸이 비었는지 보고 잠근다 (T240).
적는 것만으로는 다시 안 그려서 잠긴 채로 남았다. 다 적어 놓고 펴지를 못한다.

비었다가 찼을 때와 찼다가 비었을 때만 다시 그린다.
글자마다 다시 그리면 커서가 튄다. **그리고 다시 그린 뒤에 커서를 끝으로 되돌린다.**

## 7.9 이어달리기 (T271)

규칙서 4.1 이다. **앞의 넷과 뼈대가 다르다.**

| | 앞의 넷 | 이어달리기 |
|---|---|---|
| 가리는 것 | 있다 | **없다.** 두 화면이 같다 |
| 자리가 도는 때 | 회마다 (넷 / 하나 / 셋 / 한 바퀴) | **접힐 때마다** |
| 셈 | 한 기기에 절반 | **두 기기에 같은 수** |

### 가릴 것이 없는 판이 있다

`docs/solo_plays.md` 가 이 판을 "그대로. 가릴 것이 없다. 말로만 돈다" 고 적었다.
두 화면이 같은 것을 보이는 것이 **맞다.**

3장이 "한쪽에만 있는 것은 안 그린다" 고 정했는데 **여기는 한쪽에만 있는 것이 없다.**
규칙을 어기는 것이 아니라 그 규칙이 걸릴 자리가 없는 것이다.
격차가 뼈대인 판과 아닌 판을 같은 틀로 다루면 이 자리를 놓친다.

기기 쪽을 안 골라도 돈다. 다만 **누구 차례인지는 못 말한다.** 그것만 적고 돌린다.

### 자리가 회로 안 돈다

`roundStep` 은 지금까지 회 번호였다. 여기서는 **접은 횟수**다.
접히는 것은 사람이 정하는 일이라 미리 몇 번인지 모른다.

그리고 판 안에서 마디마다도 번갈아 간다. 둘이 겹친다.
`chnWho(folds, marks)` 가 둘을 더해 홀짝을 본다.

### 셈이 절반이 아니다

앞의 넷은 판정하는 자리가 돌아 한 기기에 절반만 남았다.
여기는 **둘이 같이 판정**하므로 두 기기에 같은 수가 남는다. `playHalf` 를 안 쓴다.

대신 화면이 **"두 기기에 같은 수가 있어야 한다. 소리 내어 견준다"** 고 적는다.
같은 수가 남는다는 것은 어긋나면 보인다는 뜻이기도 하다.

### 누가 끊었는지 물을 칸을 안 만든다

R32 가 "누가 끊었는지는 안 센다" 고 정했다.
**받을 칸이 없는 것이 그 규칙을 지키는 방법이다.** 물으면 그것이 곧 셈이 된다.

접는 단추는 하나고 누른 사람을 안 남긴다.
화면이 "접는 것은 실패가 아니라 배운 것의 경계 표시다" 라고 적는다.

## 7.10 둘이 한 문장 (T274)

규칙서 4.2 다. **가리기와 같이 판정하기가 한 판에 같이 있는 첫 판이다.**

| 판 | 가리나 | 누가 판정하나 | 셈이 절반인가 |
|---|---|---|---|
| 거울 · 한 줄 바꾸기 · 내 소리는 네가 · 전달 | 가린다 | 한쪽 | **그렇다** |
| 이어달리기 | 안 가린다 | 둘이 같이 | 아니다 |
| **둘이 한 문장** | **가린다** | **둘이 같이** | **아니다** |

여섯 판째에 이 짝이 나왔다. 그래서 이제 갈라 적을 수 있다.

**가린다고 셈이 절반이 되는 것이 아니다.
판정하는 자리가 도는 것이 절반을 만든다.**

앞의 넷은 둘이 늘 같이 왔다. 같이 오는 것만 보면 하나로 보인다.
여섯 판을 만들고 나서야 그 둘이 다른 것이라는 것이 보였다.

### 펴 보는 것이 벌이 아니다

규칙서가 못 했을 때 칸에 그렇게 적었다. 화면이 그 말을 그대로 적는다.
못 붙은 문장은 벌이 아니라 **다음에 붙일 자리**다.

펴기 단추의 이름도 "안 붙는다. 펴 본다" 다. "포기" 나 "실패" 가 아니다.
`docs/play.md` 원칙 4가 여기서 글자로 나온다.

### A등급 자료가 처음 나왔다

그리고 그 자리에서 하나가 걸렸다. `playGrade` 가 등급과 상관없이
**"Q1 소리 트랙 통과 판정에는 안 쓴다"** 를 붙이고 있었다.

A등급 자료에 그것은 **없는 금지**다. 지어낸 영어가 없는 자료인데
두 사람이 쓸 수 있는 것을 못 쓰는 것으로 읽는다.
CLAUDE.md 가 막은 것은 확신 없는 것으로 통과를 정하는 일이지 A등급이 아니다.

**부품을 만들 때 그때 있던 것만 보고 만들면 이렇게 된다.**
그때는 B등급 자료밖에 없었다. 다섯 판을 지나 A등급이 처음 오면서 드러났다.

## 7.11 겹치면 지운다 (T276)

규칙서 4.3 이다. **역할이 없는 유일한 판이다.**

### 계획표가 만들라고 한 자료가 이미 있었다

`docs/roadmap.md` 12.14 가 이 판의 첫 턴을 "단서 낱말 목록" 이라고 적어 뒀다.
규칙서의 쓰는 것 칸은 **오늘 과의 청크 목록**이고 그것은 T270 에 만들었다.

단서는 두 사람이 그 자리에서 적는 것이다. 미리 만들 목록이 아니다.
**자료 턴이 필요 없었다.** T264 와 반대 방향의 같은 일이다.
그때는 계획표가 안 쓰기로 한 것을 만들라고 했고 여기서는 이미 있는 것을 만들라고 했다.

계획표를 먼저 읽고 그대로 만들면 둘 다 헛일이 된다. **규칙서를 먼저 읽는다.**

### 역할이 없으면 자리 표시를 안 그린다

규칙서가 비워 두지 않고 "역할이 없다" 고 적었다.
**비우면 빠뜨린 것이고 적으면 정한 것이다.**

그래서 이 화면에는 "이 기기 자리" 줄이 없다. 대신 그 자리에
**"이 판은 역할이 없다. 둘이 같은 일을 한다"** 를 적는다.
없는 것도 적어야 두 사람이 앱이 덜 그린 줄 알지 않는다.

### 앱이 판정하는데 앱이 상대 낱말을 모른다

규칙서가 "앱이 글자만 견준다" 고 적었다. 그런데 두 기기는 말을 못 주고받는다.
**이 기기에는 내 낱말밖에 없다.**

반만 기계가 한다. 5장의 그 규칙이다.

| 누가 | 무엇 |
|---|---|
| 기계 | 두 낱말이 **글자로** 같은가. 그것만 |
| 사람 | 상대 낱말을 소리 내어 말하고 이 기기에 친다 |

치는 것이 번거로운 것이 아니라 **그것이 동시 공개다.**
먼저 본 사람이 자기 것을 바꾸면 이 판은 아무것도 안 재게 된다.
그래서 내 것을 다 적기 전에는 펴는 단추가 안 켜진다 (`revealGate`, T240).

### 판정이 첫 글자로 굳었다

상대 낱말 칸에 글자를 칠 때마다 판정을 다시 그려야 하는데,
T268 에서 배운 대로 **비었다가 찼을 때만** 다시 그리고 있었다.
그래서 `water` 와 `Water!` 가 "안 겹쳤다" 로 떴다. 그때 친 것은 `W` 하나였다.

칸마다 규칙이 다르다.

| 칸 | 다시 그리는 때 |
|---|---|
| 펴기가 걸린 칸 | **비었다 찼을 때만.** 잠금만 바뀐다 |
| 판정이 걸린 칸 | **글자마다.** 판정이 글자를 따라간다 |

뒤엣것은 화면 전체를 다시 그리면 커서가 튄다.
그래서 **판정 자리만 따로 두고 그 안만 다시 그린다.**

## 7.12 배속 사다리 (T280)

규칙서 6.1 이다. **숫자를 화면 코드에 안 적은 첫 판이다.**

세 칸도 3연속도 2연속도 내릴 때 할 말도 다 `ladder.js` 에서 온다.
그것은 `docs/bench_music.md` 6장에서 파생된다 (T279).

| 앞의 일곱 판 | 배속 사다리 |
|---|---|
| 쌍 여덟, 다섯 줄, 여섯 줄 같은 수를 화면 코드에 적었다 | **하나도 안 적는다** |

앞의 판들은 숫자가 규칙서에만 있고 자료에 없었다. 그래서 화면이 들고 있었다.
이 판은 규격 자체가 자료라 화면이 안 들어도 된다.
**문서를 고치면 앱이 따라 바뀐다.** 갈라질 자리가 없다.

### 같은 것을 세 번 재는 것이 아니다

배속마다 볼 것이 다르다. 0.8 은 빠뜨린 낱말, 1.0 은 박자, 1.2 는 서두른 티다.
**그 글이 세는 쪽 화면에 있어야 한다.**

없으면 셋 다 "잘했나" 가 되고 그러면 칸이 셋인 뜻이 없어진다.
`docs/bench_music.md` 6.1 이 셋으로 정한 이유가
"둘이면 통과와 실패뿐이고 셋이면 아직이라는 자리가 생긴다" 였다.

### 내리는 말도 문서에서 온다

T175 가 정한 말투 규칙이다. 사람이 아니라 **다음에 할 일**을 말한다.
"실패. 0.8 로 내려갑니다" 가 아니라 "0.8 에서 한 번 더 다진다" 다.

그 말을 화면에서 다시 짓지 않는다. `downSay` 로 문서에서 온다.
**말투 규칙도 한 자리에만 둔다.**

### 보이는 것과 세는 것을 갈랐다

문서는 `1.0` 이라고 적는데 수로만 들고 있으면 화면에 `1 배속` 이라고 뜬다.
**문서와 화면이 다른 말을 한다.**

그래서 `rate` 는 셈에 쓰고 `label` 은 눈에 쓴다. 라벨은 문서가 적은 글자 그대로다.
자료를 수로 바꾸는 자리마다 이 갈림이 생긴다.

## 8. 남은 열아홉

판 목록에 **화면이 붙은 것만 적는다.**
안 붙은 판을 적어 두면 눌러 보고 아무 일도 안 나고 그러면 앱이 고장 난 줄 안다.

목록 끝에 스무 판 중 몇이 붙었는지를 적는다.
하나뿐인 목록을 보고 이것이 전부인 줄 알면 안 된다.
나머지는 없는 것이 아니라 아직 안 만든 것이고 그동안은 규칙 카드와 종이로 돈다.

T261~T317 이 열아홉을 붙인다.

## 9. 이 화면을 무엇으로 재나 (T260)

`scripts/check_play_screen.js` 다. 두 창을 띄워 거울 판을 몬다.

### 9.1 낱말이 있나 없나로는 못 잰다

쌍의 두 낱말은 **둘 다** 짚는 쪽 화면에 있다. 있는 것이 맞다.
새는 것은 낱말이 아니라 **어느 쪽인가** 다. 그래서 이렇게 잰다.

| | |
|---|---|
| `mirTarget` 을 늘 0으로 두고 그린다 | 화면을 통째로 받아 적는다 |
| `mirTarget` 을 늘 1로 두고 그린다 | 화면을 통째로 받아 적는다 |
| 짚는 쪽 | **둘이 한 글자도 안 달라야 한다** |
| 읽는 쪽 | **둘이 달라야 한다** |

글자만 안 본다. `innerHTML` 을 견준다. 눈에 안 보이는 자리로도 샌다.
class 하나, `aria-label` 하나, `data-` 하나면 충분하다.
깨 보며 확인했다. `<span hidden data-x="...">` 하나를 심었더니 잡혔다.
**글자를 셌으면 못 잡았다.**

뒤엣줄이 앞엣줄만큼 중요하다. 읽는 쪽에서도 안 달라지면 그것은
안 새는 것이 아니라 **아무것도 안 그리는 것**이다. 그러면 이 검사가 늘 통과한다.
검사 하나에 방향이 둘 있어야 하는 자리가 이런 자리다.

### 9.2 열 번 깨서 확인했다

**통과한 검사는 아직 아무것도 말하지 않는다.** 깨야 말한다.

| 깬 것 | 잡혔나 |
|---|---|
| 짚는 쪽에 안 보이는 태그로 답을 심는다 | 잡힌다 |
| 읽는 쪽도 답을 안 본다 | 잡힌다 |
| 못 짚으면 한 번 더 없이 넘어간다 | 잡힌다 (넷) |
| 기기 쪽을 안 골라도 판을 돌린다 | 잡힌다 (둘) |
| 자리가 두 줄마다 바뀐다 | 잡힌다 (여섯) |
| 여덟을 한 대립에서만 뽑는다 | 잡힌다 |
| 절반이라는 말을 뺀다 | 잡힌다 |
| 건네는 덮개에 쌍의 낱말을 그린다 | 잡힌다 |
| 4분이 돼도 아무 말 안 한다 | 잡힌다 |
| 등급을 화면이 말 안 한다 | 잡힌다 |

### 9.3 찾은 자리에서 적는다

다섯째를 처음 깼을 때 **한 줄도 안 나왔다.** 못 잡은 줄 알았다.

잡아 놓고 안 보여 준 것이었다. 실패를 모아 뒀다가 끝에 적고 있었는데
그 뒤의 누르기가 없는 단추를 30초 기다리다 터졌다. 터지면 끝이 안 온다.

둘을 고쳤다.

| | |
|---|---|
| 적는 때 | 찾은 그 자리에서 적는다. 모아 두지 않는다 |
| 누르는 법 | 있는지 보이는지를 먼저 보고 3초만 기다린다 |

뒤엣것은 T241 과 T247 과 T254 에서 세 번 겪은 것이다.
**세 번 겪은 것을 새 검사에서 또 했다.** 그때는 앱을 몰 때였고 이번은 검사를 쓸 때다.
같은 실수가 자리를 바꿔 다시 온다.

### 9.4 판이 늘면 이 검사도 는다 (T263)

한 줄 바꾸기를 붙였다. **뼈대는 같고 다른 자리만 따로 잰다.**

| 재는 것 | 거울 | 한 줄 바꾸기 |
|---|---|---|
| 한쪽에만 있는 것 | 어느 쪽을 읽는가 | **바꿀 낱말** |
| 무엇을 흔들어 보나 | `mirTarget` 을 0과 1로 | `swpItems` 의 `to` 를 둘로 |
| 자리 | 넉 줄마다 | **한 줄마다** |
| 못 했을 때 | 한 번 더, 두 번째에 넘어감 | **알려 주고 다시 판정 안 함** |
| 자료가 모자랄 때 | - | **몇 줄인지 적고 있는 만큼** |

흔드는 방법이 판마다 다르다. 거울은 씨앗 함수를 흔들고 여기는 자료를 흔든다.
**같은 것은 재는 방식이다.** 한쪽 화면은 흔들려도 안 바뀌고 다른 쪽은 바뀐다.

넷을 깨서 확인했다.

| 깬 것 | 잡혔나 |
|---|---|
| 찾는 쪽에 안 보이는 태그로 바꿀 낱말을 심는다 | 잡힌다 |
| 알려 준 뒤에도 판정 단추를 남긴다 | 잡힌다 (넷) |
| 자리가 두 줄마다 바뀐다 | 잡힌다 (셋) |
| 모자란 과에서 몇 줄인지 안 적는다 | 잡힌다 |

### 9.5 판정하는 자리를 잰다 (T266)

내 소리는 네가를 붙였다. **판정 단추가 듣는 쪽에 있다.** 앞의 둘은 읽는 쪽이었다.

그래서 이 판에서는 **누를 것이 없는 쪽**이 어디인지를 따로 잰다.
줄마다 두 화면을 다 본다. 말하는 쪽에 단추가 있으면 실패고
듣는 쪽에 없어도 실패다.

**어기면 자기 발음을 자기가 판정하게 된다.** 그것이 이 판이 막으려던 바로 그 일이고
규칙서가 "자기 발음은 자기가 못 듣는다" 고 적어 둔 이유다.

넷을 깨서 확인했다.

| 깬 것 | 잡혔나 |
|---|---|
| 판정 단추를 말하는 쪽에도 둔다 | 잡힌다 (여섯 줄 다) |
| 말하는 쪽 화면에 안 보이는 태그로 지시를 심는다 | 잡힌다 |
| 짚어 줬다를 깨끗한 줄로 센다 | 잡힌다 |
| 기기가 하나인데 건네라고 한다 | 잡힌다 (둘) |

같이 재는 것이 하나 더 있다. **화면에 별표 두 개가 그대로 떴는지**다.
T265 에 자료에 마크다운을 넣어 두고 그것이 화면에 뜬 적이 있다.
줄마다 본다. 자료가 문서가 아니라 화면으로 간다는 것을 검사가 기억한다.

### 9.6 기계가 판정하면 검사가 셈도 본다 (T269)

전달 놀이는 **기계가 판정한다.** 앞의 셋은 사람이 했다.
사람이 판정하면 검사가 볼 것이 화면뿐이다. 기계가 판정하면 **셈도 봐야 한다.**

아는 답을 넣고 아는 수가 나오는지 본다. 여섯 자리다.

| 넣은 것 | 나와야 하는 수 |
|---|---|
| 같은 글 | 0 |
| 한 낱말만 다름 | 1 |
| 떨어진 두 자리 | 2 |
| **이어 붙은 세 낱말** | **1.** 이어 붙은 것은 한 군데다 |
| 빈 칸 | 1 |
| 대소문자와 문장부호만 다름 | 0 |

넷째가 이 셈의 값이다. 낱말로 세면 긴 토막 하나가 다섯 군데로 잡히고
그것은 **되짚을 자리가 아니다.** 되짚는 것은 자리지 낱말이 아니다.

같이 재는 것

| | |
|---|---|
| 소리 자리 | 처음 듣는 쪽에만 소리 단추. 이어폰을 묻고 반대쪽은 왜 조용한지 적는다 |
| 원문 새기 | 펴기 전에 **두 화면 어디에도** 원문이 없다 |
| 펴기 | 빈 칸에서 잠기고 **적으면 켜진다** (T268 에 안 켜졌다) |

마지막은 T268 에서 고친 자리다. 검사에 넣어 뒀으니 되돌아가면 잡힌다.
실제로 그 고침을 되돌려 깨 봤고 잡혔다.

### 9.7 가릴 것이 없는 판도 잰다 (T272)

앞의 넷은 "한쪽에만 있는 것이 정말 한쪽에만 있는가" 를 쟀다.
이어달리기는 **가릴 것이 없는 것이 맞다.** 그러면 무엇을 재나.

| 재는 것 | 왜 |
|---|---|
| 감춘 자리가 **없는가** | `.vhid` 가 뜨면 안 가릴 것을 가린 것이다 |
| 두 기기가 **같은 것**을 보이는가 | 청크 목록이 같아야 한다 |
| 두 기기가 **다른 차례**를 말하는가 | 자리는 갈린다. 화면은 안 갈린다 |
| 셈이 **절반이 아닌가** | 이 판은 두 기기에 같은 수가 남는다 |

넷째가 이 절의 값이다. 앞의 넷을 베껴 `playHalf` 를 붙이면
두 사람이 온 수를 절반으로 읽고 **두 배로 더한다.**
깨서 확인했다. 붙이면 잡힌다.

같이 재는 것

| | |
|---|---|
| 마디 세기 | 제일 긴 것이 **짧은 사슬 뒤에 줄면 안 된다** |
| 접기 | 접으면 마디가 0으로 가고 자리가 바뀐다 |
| **안 묻기** | 누가 끊었는지 고르는 자리가 화면에 없어야 한다 (R32) |

마지막은 **없는 것을 재는 검사**다. 있어야 할 것이 있는지가 아니라
없어야 할 것이 없는지를 본다. 단추에 사람 이름이 붙으면 걸린다.

### 9.8 등급이 A인 자료가 처음 왔다 (T275)

잴 것이 하나 늘었다. **A등급 자료에 "통과 판정에 안 쓴다" 가 붙으면 안 된다.**

| 자료 등급 | 그 말이 |
|---|---|
| B | **있어야 한다.** 확신 없는 것으로 통과를 정하면 안 된다 |
| A | **없어야 한다.** 없는 금지다 |

둘 다 잰다. B등급 판 넷에서 그 말이 있는지 보고 A등급 판에서 없는지 본다.
한쪽만 재면 "늘 붙인다" 와 "늘 안 붙인다" 가 둘 다 통과한다.

같이 재는 것

| | |
|---|---|
| 토막 새기 | **뒤 토막을 흔들어도 앞을 받는 쪽은 안 바뀐다** |
| 안 그리는 것이 아니라는 확인 | 앞 토막을 흔들면 앞을 받는 쪽은 바뀐다 |
| 셈 | 펴 본 문장이 **붙은 것으로 안 세어진다** |
| 마감 | 절반이라고 안 적는다. 둘이 같이 판정하는 판이다 |

넷을 깨서 확인했다. T274 의 고침을 되돌리는 것도 넣었다. 잡힌다.

### 9.9 역할이 없는 판을 잰다 (T277)

겹치면 지운다다. 잴 것이 또 다르다.

| 재는 것 | 왜 |
|---|---|
| 겹침 셈 일곱 자리 | **기계가 판정하는 판**이다. 셈을 잰다 (T269 와 같은 갈래) |
| 두 기기가 같은 맞힐 것 | 씨앗에서 나온다. 다르면 둘이 다른 판을 돈다 |
| **역할이 없다고 적혀 있는가** | 비우면 빠뜨린 것으로 읽힌다 |
| 자리 표시가 **없는가** | 앞 판을 베끼면 없는 자리가 생긴다 |

겹침 셈은 이렇게 잰다.

| 넣은 것 | 나와야 하는 답 |
|---|---|
| `water` / `water` | 같다 |
| `Water` / `water` | 같다. 대소문자는 안 본다 |
| `water!` / ` water ` | 같다. 문장부호와 빈칸도 안 본다 |
| 굽은 홑따옴표 / 곧은 것 | 같다 |
| `water` / `river` | 다르다 |
| 빈 칸 / 빈 칸 | **다르다.** 빈 것은 겹친 것이 아니다 |

마지막이 이 셈의 값이다. 둘 다 안 적었는데 겹쳤다고 하면
아무것도 안 하고 단서가 지워진다.

### 9.10 안 깨지는 자리를 깼다

처음에 **"펴기 전에 내 단서가 화면에 없어야 한다"** 를 쟀다.
깨 보려고 화면에 내 단서를 크게 그려 놓았는데 검사가 조용했다.

검사가 약한 줄 알았다. **아니었다. 없는 요구를 재고 있었다.**

이 판은 자기 단서를 자기가 본다. 감춰야 하는 것은 상대 단서인데
이 기기에는 상대 단서가 **애초에 없다.** 사람이 나중에 친다.
**기기가 격차를 만드는 판이 아니라 입으로 만드는 판이다.**

앞의 여섯 판이 다 기기가 격차를 만들었다. 그 틀을 그대로 가져와서
없는 자리를 재고 있었다. T252 의 셋째 경우다.

바꿔서 재는 것은 **펴기 전에 판정이 안 뜨는가** 다.
먼저 판정을 보면 자기 것을 바꾸게 되고 그러면 동시 공개가 아니다.
그것을 깨 봤더니 잡힌다.

### 9.11 숫자가 문서에서 오는 판을 잰다 (T281)

배속 사다리다. **검사도 숫자를 안 적는다.**
자료에서 오르내리는 수를 읽고 그 수만큼 눌러 본다.

검사에 3을 적으면 문서를 4로 고쳤을 때 검사가 빨간불이 되고,
그것은 앱이 깨진 것이 아니라 **검사가 안 따라온 것**이다.
T276 에 날짜를 박아 둔 검사가 이틀에 하루 빨간불이 되던 것과 같은 자리다.

깨서 확인한 것이 넷인데 **첫째는 안 잡히는 것이 맞는 것**이었다.

| 깬 것 | 잡혀야 하나 | 결과 |
|---|---|---|
| **문서를 "네 번" 으로 고친다** | **아니다.** 앱과 검사가 같이 따라와야 한다 | 안 잡힌다 |
| 화면이 오르는 수를 3으로 박는다 | 그렇다. 문서와 갈라졌다 | 잡힌다 |
| 내릴 때 "실패" 라고 적는다 | 그렇다 (T175) | 잡힌다 (둘) |
| 칸마다 볼 것을 같게 만든다 | 그렇다. 칸이 셋인 뜻이 없어진다 | 잡힌다 |

첫째가 이 검사의 값이다. **문서 하나를 고치면 셋이 같이 움직인다.**
문서와 앱과 검사가 한 줄로 이어져 있다는 것을 그 자리에서 보였다.

### 9.12 넓게 보면 엉뚱한 자리를 문다

내릴 때 "실패" 라고 적는지를 처음에는 **화면 전체**에서 찾았다.
등급 설명문의 "못 찾으면 실패로 낸다" 가 걸렸다.

그것은 파생기가 하는 일을 적은 말이지 **학습자에게 하는 말이 아니다.**
내리는 그 자리(`#ladTurn`)만 보게 고쳤다. T249 와 같은 갈래다.

### 9.13 기계가 안 보는 것

**상 건너로 보이는 화면**과 **소리가 정말 갈렸는지**다.
앞엣것은 가림 단추가 있고 (T244) 뒤엣것은 이어폰을 묻는다 (T245).
둘 다 사람이 하는 일이라 여기서 못 잰다. 검사가 그것을 적고 끝낸다.

## 10. 시계가 한 장에 두 번 서는 판 (T283)

3초 벽이다. 앞의 여덟 판과 다른 자리가 넷이다.

### 10.1 시계가 판의 뼈대다

앞의 판들에서 시계는 **블록이 언제 끝나는가**를 알려 주는 물건이었다.
4분이나 5분이고 다 되면 그 판이 끝난다.

이 판의 시계는 다르다. **한 장마다 서고 그 초가 카드마다 다르다** (2 3 5 8 10초).
다 되는 것이 끝이 아니라 **상대가 대신 받는 자리로 넘어가는 것**이다.
그래서 한 장에 시계가 두 번 선다. 같은 초를 준다.
대신 받는 쪽에 시간을 덜 주면 그것이 벌이 된다.

블록 시계는 안 뒀다. 규칙서 6.2 의 끝 조건이 **장수**이지 분이 아니다.
배속 사다리는 "5분이 되면 그 칸에서 끝난다" 가 규칙서에 있어 시계를 뒀고
이 판에는 그 줄이 없다. **없는 끝을 앱이 만들지 않는다.**

초를 분으로 안 적는다. 제일 짧은 카드가 2초라 `0:02` 로 적으면
숫자 하나를 보려고 네 글자를 읽게 된다.

### 10.2 시계가 한 화면에서만 간다

두 기기가 각자 재면 두 시계가 몇 초씩 어긋난다. 시작을 각자 누르기 때문이다.
어긋난 시계로 압박을 만들면 **받는 쪽이 자기 시계를 보고 스스로 판정한다.**
그것이 이 판이 막으려던 일이다.

카드가 "재고 시작 신호를 준다" 라고 적었고 규칙서의 판정 칸도 띄운 사람이다.
그래서 **시계도 판정 단추도 띄우는 쪽 화면에만** 있다.
받는 쪽 화면에는 제한시간 숫자가 서 있고 "띄우는 쪽이 잰다" 라고 적혀 있다.

### 10.3 판정 단추는 없고 넘기는 단추는 있다

회는 기기마다 자기가 센다 (`round.md` 6장). 받는 쪽에 아무 단추도 안 두면
그 기기만 첫 장에 남고 판 표시 두 글자가 갈린다.

그래서 받는 쪽에 **다음 장**을 둔다. 셈은 안 건드린다.
그리고 **둘 다 못 받았다고 한다**를 하나 더 둔다. 이것은 판정이 아니다.
띄운 쪽이 말한 것을 이 기기에도 적는 일이다.

**안 적으면 다음 판에 두 기기가 다른 장을 낸다.**
미룬 장이 다음 판의 앞으로 오는데 그 목록이 한 기기에만 있으면
두 덱이 갈린다. 기기끼리 말할 길이 없으니 사람이 옮긴다.

### 10.4 덱이 판 도중에 안 섞인다

미룬 것을 앞에 놓는 셈을 **그릴 때마다** 다시 하면 여섯째 장을 미루는 순간
그 장이 첫째가 되고 이미 돈 다섯 장이 뒤로 밀린다.

그래서 첫 장을 그릴 때 한 번 정해 `deck` 에 적어 둔다.
`처음부터` 가 그것만 지운다. **미룬 목록은 안 지운다.**
그때 미룬 것이 앞으로 온다. 규칙서가 "다음 판으로 미룬다" 라고 적은 것이 그 말이다.

### 10.5 안 여는 날이 있는 첫 판

첫 다섯 강은 그날까지 쌓인 단서가 열이 안 된다 (T282).
화면이 몇 장인지와 왜 안 여는지를 적고 **다른 판을 돌라고 말한다.**

빈 화면을 두거나 있는 만큼 돌리는 것과 다르다.
둘이 한 문장은 "있는 만큼 돈다" 로 갔는데 (T274) 이 판은 그렇게 못 한다.
**모자란 것을 채우려면 같은 장을 두 번 내야 하고 두 번째는 압박이 아니다.**
판마다 모자랄 때 할 일이 다르다. 자료가 무엇을 하는 물건인가에 달렸다.

## 11. 시계와 덱을 재는 검사 (T284)

`check_play_screen.js` 에 판정 묶음 열셋을 붙였다. 62~74 다.
앞의 여덟 판과 다른 자리 넷을 잰다.

| 무엇 | 어떻게 |
|---|---|
| 단서가 한쪽에만 | 받는 쪽 `innerHTML` 에 재료 문장이 한 글자도 없어야 한다 |
| 정답도 한쪽에만 | **정답이 붙은 장을 찾아 넣는다.** 덱에 없으면 끌어온다 |
| 시계가 한쪽에만 | 받는 쪽에 `#walGo` 와 `#walHit` 가 없고 `#walNext` 는 있다 |
| 대신 받기 | 시간이 다 되면 넘어가지 않고 같은 초를 다시 준다 |
| 미룬 장 | 판 도중에 덱이 안 섞이고 다음 판이 그것부터 낸다 |
| 두 기기 | 미룬 목록이 두 기기에 같이 남는다 |
| 안 여는 날 | 판이 안 열리고 **낼 수 있는 단서가 하나도 안 뜬다** |

### 11.1 여기도 숫자를 안 적었다

T281 에 정한 그대로다. 열 장도 다섯 장도 윗선도 `DATA.wall` 에서 읽고
그만큼 누른다. 규칙서 6.2 의 끝 조건을 여덟으로 고쳐 봤더니
파생기와 앱과 검사가 **셋 다 같이 따라왔다.** 실패가 안 났다. 그것이 맞다.

### 11.2 없으면 안 잰 것이 된다

정답이 붙은 장은 94장 중 열아홉이고 그날 덱 열 장에 하나도 안 들 수 있다.
안 들면 이 자리가 **아무것도 안 재고 조용히 통과한다.**

그래서 덱에서 못 찾으면 자료 전체에서 하나 끌어와 덱 앞에 놓는다.
그리고 띄우는 쪽 화면에는 **있는지**도 같이 본다.
안 그리는 것은 안 새는 것이 아니다. T260 에 거울 판에서 배운 그 줄이다.

### 11.3 한 장만 보면 운에 기댄다

안 여는 날 검사가 처음에는 `cards[0].mat` 하나만 봤다.
덱은 섞여 나오므로 첫 장이 화면에 안 뜰 수도 있고 **그러면 안 뜬 것이 아니라
다른 장이 뜬 것이다.** 판이 낼 수 있는 것을 다 보게 고쳤다.

고치고 나서 깨 보니 `asked / walked / book` 이 떴다. 첫 장이 아니었다.
**고치기 전에도 그 깸은 다른 줄이 잡아 줬다.** 잡아 준 줄이 있다고
약한 줄을 그대로 두면 그 줄이 혼자 서는 날 아무것도 안 잰다.

### 11.4 열 가지를 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 재료를 받는 쪽에도 그린다 | 잡힘 |
| 정답을 받는 쪽에도 그린다 | 잡힘 |
| 판정 단추를 받는 쪽에도 둔다 | 잡힘 (넷) |
| 자리를 한 장마다 바꾼다 | 잡힘 |
| 시간이 다 되면 바로 다음 장으로 | 잡힘 (셋) |
| 대신 받은 것을 안 센다 | 잡힘 (둘) |
| 덱을 안 얼린다 | 잡힘 |
| 처음부터가 미룬 목록도 지운다 | 잡힘 (둘) |
| 받는 쪽 미룸 단추를 없앤다 | 잡힘 (셋) |
| 안 여는 날에 있는 만큼 돌린다 | 잡힘 (셋) |
| **문서를 여덟 장으로 고친다** | **안 잡힘. 그것이 맞다** |

## 12. 자료가 같고 재는 것이 다른 두 판 (T285)

되받아치기다. 이어달리기와 **같은 `chunks.js` 를 쓴다.**
둘 다 연속을 세고 둘 다 끊기면 자리가 바뀐다. 그런데 화면이 다르다.

| | 이어달리기 (T271) | 되받아치기 (T285) |
|---|---|---|
| 재는 것 | 말이 **되는가** | **쉼이 없는가** |
| 판정 | 둘이 같이 | **던진 사람** |
| 단추 | 두 화면에 같은 것 | **한 화면에만** |
| 자리가 도는 계기 | 접힐 때 | 쉼이 날 때 |
| 셈 합치기 | 두 기기에 같은 수 | **두 기기 중 큰 것** |
| 분 | 5 | 4 |

**자료가 같다고 화면이 같지 않다.** 무엇을 재는가가 화면을 정한다.

### 12.1 셈을 합치는 법이 세 가지가 됐다

| 법 | 어느 판 | 왜 |
|---|---|---|
| 더한다 (`playHalf`) | 거울, 한 줄 바꾸기, 내 소리는 네가, 둘이 한 문장, 3초 벽 | 판정하는 자리가 판 안에서 돌아 한 기기에 절반만 남는다 |
| 같은 수다 | 이어달리기 | 둘이 같이 판정하므로 두 기기에 같은 수가 남는다 |
| **큰 것을 든다** | 되받아치기 | 각자 **자기가 던진 동안**의 제일 긴 연속을 든다 |

셋째가 여기서 처음 나왔다. 더하면 안 된다.
규칙서가 남기라는 값이 "한 번에 제일 많이 주고받은 수" 이지
두 사람이 주고받은 것을 다 더한 수가 아니다.

**화면이 그 말을 안 하면 두 사람이 더한다.** 앞의 다섯 판에서 더하라고 배웠기 때문이다.
그래서 마감 화면에 "더하지 않는다" 를 적었다.

### 12.2 안 세는 것과 0인 것은 다르다

처음에 큰 수를 두 화면에 다 뒀다. 받는 쪽 화면에 **0 번 주고받았다**가 크게 떴다.

둘이 실제로 주고받는 동안 한쪽 화면이 0이라고 말하는 것이다.
그 기기가 안 세는 것이지 주고받은 것이 0인 것이 아니다.

받는 쪽에서는 큰 수를 빼고 "지금은 던진 쪽 화면이 센다" 로 바꿨다.
**빈 자리에 0을 두면 0이 값이 된다.**

## 13. 다른 자리만 재는 검사 (T286)

되받아치기다. 자료가 이어달리기와 같으니 **자료를 다시 안 잰다.**
잴 것은 두 판이 갈리는 자리 넷이다. 판정 묶음 75~83 이다.

| 무엇 | 어떻게 |
|---|---|
| 단추가 한쪽에만 | 받는 쪽에 `#rbdOn` `#rbdStop` 이 없고 `#rbdSaid` 는 있다 |
| 큰 수도 한쪽에만 | 받는 쪽에 `.chnbig` 가 없다. **안 세는 것과 0인 것은 다르다** |
| 청크가 안 걸러진다 | 화면의 조각이 오늘 과의 목록 **전부**여야 한다 |
| 쉼마다 자리 | 주고받기만 여러 번 해도 자리가 안 돈다 |
| 셈이 큰 것이다 | 마감 화면이 **더하라고 적으면 실패다** |

### 13.1 없어야 할 말을 찾는 판정

앞의 아홉 판은 있어야 할 말을 찾았다. 이 판에는 그 반대가 하나 있다.

```js
if (/소리 내어 더한다|그 절반이다/.test(rbDone))
  no("되받아치기: 마감 화면이 더하라고 적는다. 이 판의 값은 큰 것 하나다");
```

`playHalf` 가 내는 말이다. 앞의 다섯 판이 그것을 쓰고 여기서 쓰면 틀린다.
**같은 부품이 어느 판에서는 맞고 어느 판에서는 틀리다.**
부품을 부르는 자리를 검사가 봐야 하는 이유가 이것이다.

깨서 확인했다. `playHalf` 로 바꿔 놓으니 판정 넷이 같이 떴다.

### 13.2 옆줄이 잡아 준 것을 다시 봤다

자리가 회마다 도는 깸을 넣었더니 판정 78의 누르기가 먼저 터졌다.
**그 줄이 잡아 준 것이지 자리 검사가 잡은 것이 아닐 수 있다.**

T284 에서 겪은 자리라 따로 세어 봤다. 판정 81 이 제 이름으로 네 번 떴다.
혼자 서도 잡는다. **한 번 겪은 자리는 다음에 세어 보고 넘어간다.**

### 13.3 여덟을 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 판정 단추를 두 쪽에 다 둔다 | 잡힘 (넷) |
| 마감에서 `playHalf` 를 쓴다 | 잡힘 (넷) |
| 쉼이 나면 제일 긴 것도 지운다 | 잡힘 |
| 청크를 걸러 보인다 | 잡힘 (열둘을 다 적는다) |
| 받는 쪽 단추가 셈을 건드린다 | 잡힘 |
| 자리가 주고받을 때마다 돈다 | 잡힘 (넷) |
| 받는 쪽에도 큰 수를 둔다 | 잡힘 |
| 받는 쪽 단추를 없앤다 | 잡힘 (넷) |

## 14. 이름은 주고 값은 안 준다 (T289)

한 사람만 본다다. 열한째 판이고 화용 트랙의 첫 판이다.

앞의 열 판은 가리는 것이 **한 덩어리**였다. 낱말이거나 줄이거나 카드였다.
이 판은 **한 덩어리를 반만 가린다.**

| 알아내는 쪽 화면 | |
|---|---|
| 요소 **이름** (관계, 목적) | **있다** |
| 그 **값** (서비스 상황, 요청) | 없다 |

값이 뜨면 알아낼 것이 없고 이름도 안 주면 무엇을 물을지 모른다.
이름 없이 하면 판이 아니라 스무고개가 된다. **그 사이가 이 판의 자리다.**

### 14.1 안 그리는 코드가 없다

T288 에 파생기가 B면을 아예 안 담았다. 그래서 이 화면에는
**B면을 안 그리는 코드가 없다.** 그릴 것이 없다.

가리는 코드가 없으면 가리기를 잊을 수도 없다. 자료에서 뗀 값이 여기서 나온다.

### 14.2 낸 요소가 날을 넘는다

이 판만 `playRec` 밖에 기록을 둔다. `S.situ` 다.

그날 셈은 날마다 새로 나는데 **어느 요소를 이미 냈는지는 날이 바뀌어도 남아야 한다.**
안 남기면 Q1 열 장이 열흘에 바닥난다 (D1).

2 + 2 + 1 로 세 판에 나뉜다. 셋째 판은 하나만 낸다.
`need` 가 둘인데 남은 것이 하나면 **있는 만큼 낸다.** 그것이 셋째 판이다.

### 14.3 알아내는 쪽도 요소를 뺀다

세 단추가 다 요소를 뺀다. 쥔 쪽의 둘과 알아내는 쪽의 하나다.

한쪽만 빼면 **내일 두 화면이 서로 다른 요소를 알아내라고 적는다.**
3초 벽의 미룬 목록과 같은 자리다 (T283). 날을 넘는 기록은 두 기기가 각자 갖고
기기끼리 말할 길이 없으니 **사람이 옮긴다.**

### 14.4 안 나온 것과 다 접은 것을 다르게 적는다

| 화면 | 언제 |
|---|---|
| "아직 이 판은 안 연다. **적어서가 아니라 아직 안 나와서다**" | 19강까지 |
| "오늘까지 나온 N장을 **다 접었다**" | 다섯 요소를 다 낸 뒤 |

앞엣것은 기다리면 오고 뒤엣것은 안 온다.
같은 말로 적으면 두 사람이 오지 않을 것을 기다린다.

### 14.5 틀에 안 맞는 값을 틀에 넣지 않았다

마감에서 `playHalf` 를 쓰려다 뺐다. 그 부품의 말은 "N장 중 몇" 꼴이고
이 판의 값은 **장마다 몇 번 물었나** 라 셀 수 있는 하나가 아니다.

넣어 보니 화면이 "규칙서가 남기라는 값은 **장마다 물은 수 중 몇**이다" 라고 적었다.
T286 에 되받아치기에서 `playHalf` 를 빼면서 배운 것과 같다.
**부품이 옳은지가 아니라 그 자리에서 옳은지를 본다.**

### 14.6 마감을 두 번 적었다가 합쳤다

시계가 다 됐을 때와 덱을 다 돌았을 때 둘 다 마감이다.
두 자리에 같은 글을 적어 놓고 **한쪽만 고쳤다.** 그래서 한쪽 화면에만
"물은 수는 없다 다" 가 남아 있었다.

`oneDone` 하나로 묶었다. **두 번 적으면 한쪽만 고치는 날이 온다.**

## 15. 약하게 짠 판정을 깨 보고 알았다 (T290)

한 사람만 본다 판정 검사다. 판정 묶음 84~95 다.

| 무엇 | 어떻게 |
|---|---|
| 값이 안 샌다 | 알아내는 쪽 `innerHTML` 에 다섯 값이 한 글자도 없다 |
| 이름은 있다 | **안 그리는 것과 안 새는 것은 다르다.** 이름이 없으면 스무고개다 |
| 2 + 2 + 1 | 같은 카드를 `most` 번 돌려 낸 요소가 안 겹치는지 본다 |
| 두 기기 | 낸 요소가 두 기기에 같이 남는다 |
| 못 여는 날 둘 | 아직 안 나온 날과 다 접은 날이 **다른 말을 한다** |
| 마감 | `playHalf` 틀을 쓰면 실패다 (T289) |

### 15.1 한 번 넘겨서는 안 잡힌다

덱이 판 도중에 안 섞이는지를 처음에는 이렇게 쟀다.

```js
const oDeck0 = ...;  await tap(A, oBtn, "한 장 넘긴다");
if (deckNow !== oDeck0) no("덱이 섞였다");
```

**깨 보니 안 잡혔다.** 한 장을 그냥 넘기면 그 카드는 요소가 남아 덱에 그대로 있다.
덱이 흔들리는 것은 **카드가 덱에서 빠질 때**다.

첫 카드를 한 판이면 바닥나게 만들어 놓고 넘기게 고쳤다. 그러니 잡혔다.

T284 에 "옆줄이 잡아 준다고 약한 줄을 두면 안 된다" 를 적었다.
이번은 옆줄도 없었다. **약한 줄이 혼자 서 있었고 아무것도 안 재고 있었다.**
깨 보지 않았으면 통과로 남았다.

### 15.2 묶음이 안 붙은 것을 알아보게 했다

판 화면은 `english.html` 밖의 **한 묶음**이다 (T259).
열한 판 중 아무 판에 오타가 나면 묶음이 통째로 안 읽히고
검사는 거울 판을 기다리다 8초 만에 터진다.

그때 나오던 말이 `Timeout 8000ms exceeded` 하나였다. **거울 판이 깨진 줄 안다.**
T289 에 실제로 그 자리에 섰다. 따옴표 하나가 어긋났는데 거울 판을 봤다.

이제 붙은 판 수를 세어서 적는다.

```
[실패] 판 묶음이 안 붙었다. 붙은 판이 0개다.
       **0이면 어느 판의 오타든 여기서 터진다.** 거울 판만의 일이 아니다.
```

**검사가 틀린 곳을 가리키면 안 잡는 것보다 나쁠 때가 있다.**
안 잡히면 찾아보는데 틀린 곳을 가리키면 거기만 본다.

### 15.3 여덟을 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 값을 알아내는 쪽에도 그린다 | 잡힘 |
| 알아내는 쪽이 요소를 안 뺀다 | 잡힘 |
| 못 닿아도 요소를 안 뺀다 | 잡힘 (셋) |
| 마감에 `playHalf` 를 쓴다 | 잡힘 (둘) |
| 덱을 안 얼린다 | **처음엔 안 잡혔다.** 판정을 고쳐서 잡았다 |
| 다 접은 날에 안 나왔다고 적는다 | 잡힘 (둘) |
| 판정 단추를 알아내는 쪽에도 둔다 | 잡힘 (셋) |
| 판 묶음에 오타를 낸다 | **잡히고 어디를 볼지도 적는다** |

## 16. 사람이 자리를 대고 앱이 칸수를 센다 (T292)

파장이다. 열두째 판이다.

규칙서 7.2 의 판정 칸이 "**세기를 쥔 사람.** 한 칸 안이면 닿은 것으로 본다" 다.
사람이 판정한다고 적혀 있는데 **몇 칸 벌어졌나는 셈이다.**

그래서 갈랐다.

| 누가 | 무엇을 |
|---|---|
| 맞히는 쪽 | 눈금에서 한 자리를 짚고 그 수를 소리 내어 말한다 |
| 쥔 쪽 | 들은 수를 자기 기기에 **댄다** |
| 앱 | 몇 칸인지를 **센다** |

사람이 하는 것은 **무엇을 짚었는가**이고 앱이 하는 것은 **몇 칸인가**다.
겹치면 지운다에서 앱이 글자만 견주는 것과 같은 자리다 (T276).

**앱이 판정하는 판이 하나 더 늘었다고 적지 않는다.** 판정은 사람이 한다.
앱은 뺄셈을 한다. 그 둘을 같은 말로 적으면 규칙서 9장의 셈이 틀어진다.

### 16.1 감추는 것이 줄이 아니라 세기다

앞의 열한 판은 감추는 것이 **말할 거리**였다. 낱말이거나 줄이거나 카드였다.
이 판은 말할 거리를 둘 다 본다. **감추는 것은 그것을 어떻게 말하는가다.**

오늘 과의 줄을 `relay.js` 에서 가져와 둘 다 보인다.
그 줄을 다섯 세기로 고쳐 말하는 것이 이 판이고 그중 어느 세기인지만 한쪽에 있다.

### 16.2 두 칸 넘게는 점을 안 넘긴다

규칙서가 "두 칸 넘게 벌어지면 A가 어디였는지 보여 주고 **다시 말한다**" 라고 적었다.
그래서 그 자리에서는 회가 안 는다. 어디였는지를 적고 그 점에 머문다.

**다시 말하는 것이 벌이 아니라고 화면이 적는다.** 마감에도 그 수를 남긴다.
그 자리가 두 사람 사이에서 제일 안 보인 자리다.

### 16.3 넣은 자리를 눈으로 가른다

눈금 다섯 중 1과 3과 5는 카드에 있는 값이고 2와 4는 넣은 자리다 (T291).
화면이 그 둘을 다르게 그린다. 넣은 자리에는 **(넣은 자리)** 라고 적는다.

**있는 것과 내가 얹은 것을 화면에서도 가른다.** 등급 한 줄로만 적으면
두 사람이 다섯 칸을 다 같은 무게로 읽는다.

### 16.4 같은 자리를 세 번째로 만났다

마감이 `playHalf` 에 "6점 중 몇" 을 넘겨 화면이 "**6점 중 몇 중 몇이다**" 라고 적었다.
T286 과 T289 에 이어 세 번째다.

이번엔 부품을 안 뺐다. 이 판의 값은 "여섯 중 몇" 이라 그 틀에 맞는다.
넘길 글에서 "중 몇" 만 뺐다.

**부품이 맞는 자리인지와 넘기는 글이 맞는지가 다른 물음이었다.**
앞의 두 번은 부품이 안 맞았고 이번은 부품은 맞고 글이 겹쳤다.

## 17. 세기를 감추는 판을 재는 검사 (T293)

파장 판정 검사다. 판정 묶음 96~106 이다.

| 무엇 | 어떻게 |
|---|---|
| 세기가 한쪽에만 | **거울 판의 자를 그대로 든다** (T260). 세기를 고정하고 두 번 그려 견준다 |
| 줄은 둘 다 | 감추는 것이 줄이 아니다. 줄이 한쪽에만 있으면 실패다 |
| 눈금도 둘 다 | 짚을 자리가 안 보이면 못 짚는다 |
| 넣은 자리 | 2와 4가 화면에서 갈린다 (T291) |
| 세 가지 폭 | `near` 안 / `near` 넘고 `far` 이하 / `far` 넘게. **셋이 다 다르게 돈다** |

### 17.1 거울 판의 자가 열두 판 만에 다시 맞았다

T260 에 거울 판에서 만든 자다. 답을 고정하고 두 번 그려 `innerHTML` 을 통째로 견준다.
그 뒤로 열 판을 붙이는 동안 안 썼다. 가리는 것이 낱말이나 줄이라
**있나 없나**로 잴 수 있었기 때문이다.

이 판은 다르다. 감추는 것이 **세기**이고 세기는 눈금에 다섯 칸이 다 보인다.
어느 칸이 이 점인가만 안 보인다. 그러면 있나 없나로는 못 잰다.

그래서 그 자를 다시 들었다. **자를 만든 자리와 쓸 자리가 열두 판 떨어져 있었다.**

### 17.2 폭이 셋이라 판정도 셋이다

| 벌어진 칸 | 무엇이 일어나나 |
|---|---|
| `near` 이하 | 닿은 것으로 세고 점이 넘어간다 |
| `near` 초과 `far` 이하 | 안 세지만 점은 넘어간다 |
| `far` 초과 | **점이 안 넘어간다.** 어디였는지 보여 주고 다시 말한다 |

가운데 칸을 안 재면 "닿았나 아니냐" 두 가지만 재는 것이 된다.
**규칙서가 셋으로 갈라 놓은 것을 검사가 둘로 합치면 그 사이가 안 잡힌다.**

### 17.3 여덟을 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 세기를 맞히는 쪽에도 그린다 | 잡힘 (네 칸) |
| 줄을 쥔 쪽에만 그린다 | 잡힘 |
| 두 칸 넘게도 점을 넘긴다 | 잡힘 (셋) |
| 정확히 맞아야 닿은 것으로 센다 | 잡힘 |
| 넣은 자리를 안 가른다 | 잡힘 |
| 단추를 맞히는 쪽에도 둔다 | 잡힘 (셋) |
| 마감에서 절반이라는 말을 뺀다 | 잡힘 |
| 자료에 마크다운을 되돌린다 | **화면 검사와 자료 검사가 둘 다 잡았다** |

### 17.4 누르는 참을성을 3초에서 8초로 늘렸다

깨 보는 중에 3초 벽과 되받아치기에서 한 번 흔들렸다. 눌리는 것을 못 눌렀다.
지금 나무로 네 번 돌려 네 번 다 초록불이었다.

3초는 T260 에 정한 값이고 그때 막으려던 것은 **없는 것을 누르며 30초를 기다리는 일**이다.
그것은 누르기 전에 있는지 보는 줄이 이미 막는다.
남은 3초는 기계가 바쁠 때 **눌리는 것을 못 누른 것으로 만든다.**

**다시 돌리면 초록불이 되는 빨간불이 제일 나쁘다** (T280).
진짜 빨간불을 흔들리는 것으로 읽게 만든다.

## 18. 답을 맞히는 코드가 없는 판 (T295)

누구 말이야다. 열셋째 판이다.

`whose.js` 에는 register 가 없다 (T294). 담고 안 그리는 것이 아니라 안 담았다.
**그래서 이 화면에는 답을 맞히는 코드가 없다.** 견줄 것이 없다.

앱이 하는 것은 자리를 내는 것과 둘이 같았는지를 적는 것뿐이다.
맞았는지는 아무도 모른다.

| 판 | 앱이 하는 것 |
|---|---|
| 겹치면 지운다 (T276) | 글자를 견준다 |
| 파장 (T292) | 칸수를 센다 |
| **누구 말이야** | **아무것도 안 견준다** |

앞의 둘은 사람이 낸 것을 앱이 셌다. 이 판은 셀 것이 없다.
**격식은 답이 하나가 아니다.**

### 18.1 갈린 것이 판 밖으로 나간다

못 했을 때 칸이 "갈렸다고 적고 넘어간다. **그 자리가 주 7일째 점검에 간다**" 다.

그래서 이 판만 `S.wsplit` 에 남긴다. 그날 셈이 아니라 **그 주의 것**이다.
주간 점검 화면이 그것을 읽어 5단계 위에 띄운다.

**판 하나가 다른 화면에 값을 내보내는 첫 자리다.** 앞의 열두 판은
`playRec` 안에서 끝났다. 그날이 지나면 아무것도 안 남았다.

주간 점검의 규칙이 그것을 받는다 (T226). **앱이 세고 사람은 왜 그랬는지를 적는다.**
갈린 자리는 앱이 세고 무엇을 물을지는 사람이 5단계에 적는다.

### 18.2 셈이 절반이 아닌 두 번째 판

기록할 값이 "둘의 생각이 **같았던** 벌" 이다. 한 사람의 값이 아니다.
그래서 두 기기에 같은 수가 남는다. 이어달리기와 같다 (T271).

셈을 합치는 법이 이제 셋이고 판마다 어느 것인지가 갈린다.

| 법 | 몇 판 |
|---|---|
| 더한다 (`playHalf`) | 거울, 한 줄 바꾸기, 내 소리는 네가, 둘이 한 문장, 3초 벽, 한 사람만 본다, 파장 |
| 같은 수다 | 이어달리기, **누구 말이야** |
| 큰 것을 든다 | 되받아치기 |

### 18.3 판정하는 자리와 적는 자리가 다르다

판정은 판정하는 쪽이 한다. 그런데 **적는 것은 둘 다 한다.**

고르는 쪽 화면이 "그쪽이 말한 것을 이 기기에도 적는다. 안 적으면
주 점검에 갈 자리가 한 기기에만 남는다" 라고 적는다.

T283 과 T289 에서 만난 그 자리다. **앱 안에서 갈리는 자리는 두 기기 사이에서도 갈린다.**
이번은 그 값이 하루를 넘어 그 주까지 간다. 갈리면 더 오래 남는다.

## 19. 판 밖으로 나간 값을 재는 검사 (T296)

누구 말이야 판정 검사다. 판정 묶음 107~118 이다.

| 무엇 | 어떻게 |
|---|---|
| 정답이 없다 | **자료를 본다.** 화면만 보면 담고 안 그리는 것과 구별이 안 된다 |
| 감출 것이 없다 | 쓸 자리도 세 단추도 두 화면에 다 있다 |
| 고르기 전 | 판정 단추가 안 뜬다 |
| 그 주에 남는다 | 갈린 자리가 그날이 아니라 **그 주**에 남는다 |
| 두 기기 | 두 목록이 같다 |
| **주 점검** | 판 탭을 떠나 `#weekCheck` 를 읽는다 |

### 19.1 판 탭을 떠나서 재는 첫 판정

이 판은 값을 판 밖으로 내보낸다 (T295). 그러면 **나간 자리에서도 재야 한다.**

여기서 안 재면 규칙서 7.3 의 "그 자리가 주 7일째 점검에 간다" 가 글로만 남는다.
`S.wsplit` 에 넣고 `go("ledger")` 로 옮겨 그 글이 뜨는지 본다.
없는 날에는 안 뜨는지도 같이 본다. **한쪽만 재면 늘 뜨는 것과 구별이 안 된다.**

### 19.2 화면을 두 때에 본다

정답을 주는지를 처음에는 **고르기 전 화면**만 봤다.
판정 단추는 고른 뒤에만 뜬다. 그래서 단추 글을 "맞았다" 로 바꿔 놔도 안 잡혔다.

깨 보고 알았다. 고르기 전과 고른 뒤를 다 본다.
**화면이 때에 따라 달라지는 판에서는 검사도 때를 따라가야 한다.**

### 19.3 깨지지 않는 것을 지키는 코드를 뺐다

덱을 얼리는 자리를 3초 벽(T283)과 한 사람만 본다(T289)에서 가져와 넣어 뒀다.
**깨 봐도 안 깨졌다.**

그 둘은 판을 도는 동안 카드가 덱에서 빠진다. 미루면 앞으로 오고 요소를 다 내면 사라진다.
이 판은 빠지는 것이 없다. `roundOrder` 가 같은 씨앗에 같은 차례를 내므로 몇 번 세도 같다.

**얼리는 코드와 그것을 재는 판정을 둘 다 뺐다.**
깨지지 않는 것을 지키는 코드는 지키는 일을 안 하고 **지키는 것처럼 보이기만 한다.**
T290 에 약한 판정을 고쳤는데 이번은 **판정이 아니라 지켜지는 쪽이 없었다.**

대신 잴 수 있는 것을 넣었다. 덱이 오늘 강에 매여 있는가다.

### 19.4 여덟을 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 주 점검이 갈린 자리를 안 읽는다 | 잡힘 (둘) |
| 갈린 자리를 그날에 남긴다 | 잡힘 |
| 마감에 `playHalf` 를 쓴다 | 잡힘 (셋) |
| 고르기 전에도 판정 단추를 띄운다 | 잡힘 (둘) |
| 쓸 자리를 한쪽에만 그린다 | 잡힘 (둘) |
| 단추 글을 "맞았다" 로 바꾼다 | **처음엔 안 잡혔다.** 때를 늘려 잡았다 |
| 자료에 register 를 되돌린다 | 파생기가 잡았다 |
| 덱을 안 얼린다 | **안 잡혔고 그것이 맞았다.** 얼릴 것이 없었다 |

## 20. 감출 것이 하나씩 양쪽에 있는 판 (T298)

못 알아들은 척이다. 열넷째 판이고 repair 트랙의 첫 판이다.

앞의 열세 판은 **한쪽이 더 보고 한쪽이 덜 봤다.** 이 판은 다르다.

| 자리 | 보는 것 | 못 보는 것 |
|---|---|---|
| 뭉개는 쪽 | 대본 줄 | **어느 세기로 되물으라고 했는지** |
| 되묻는 쪽 | 되묻기 세기 | **무슨 줄인지** |

줄을 둘 다 보면 뭉갠 자리를 귀로 안 찾고 눈으로 찾는다.
세기를 둘 다 보면 뭉개는 쪽이 그 세기에 맞춰 뭉갠다.
**감출 것이 하나씩 양쪽에 있다.**

그래서 이 판의 판정 검사는 **두 쪽을 다 봐야 한다.** 한쪽만 보면 절반만 잰다.

### 20.1 보기는 벌이 아니라 사다리다

못 했을 때 칸이 "되묻는 말이 안 나오면 화면이 그 세기의 보기를 보여 준다.
보고 말한다" 다.

보기를 여는 것이 실패가 아니다. **셈에서 빠질 뿐이다.**
화면이 두 자리에 그 말을 적는다. 여는 단추 옆과 마감 화면이다.

안 적으면 두 사람이 보기를 안 열고 넘어간다.
**빈손으로 넘어간 줄과 보고 말한 줄은 다르고 뒤엣것이 낫다.**

### 20.2 보기가 없는 단이 화면에 나온다

대본 52과에 전체를 다시 말해 달라는 되묻기가 **하나도 없다** (T297).
그 세기가 걸리면 화면이 그렇게 적는다.

```
이 세기는 대본에 보기가 없다. 52과 어디에도 전체를 다시 말해 달라는
되묻기가 없다. 지어내지 않고 없다고 적는다. 둘이 만들어 본다.
실제 말에서 제일 센 것은 드물다는 것이 여기서 보인다.
```

**빈 자리를 안 채운다.** 채우려면 지어내야 하고 그것이 1순위 규칙이 막는 일이다.
그리고 그 빈 자리가 두 사람에게 알려 주는 것이 있다.
기준서 13.1 이 "제일 센 되묻기만 쓰면 무례가 된다" 고 걱정했는데
실제 말에서는 그것이 애초에 드물다.

**없다는 것을 화면이 말하는 첫 자리다.** 앞의 판들은 모자라면 "있는 만큼 돈다"
거나 "안 연다" 였다. 이 판은 **없다는 것 자체를 보여 준다.**

### 20.3 보기에 어디서 왔는지를 붙인다

```
Do you mean drive a race car? · lle1-20
```

과 이름을 옆에 둔다. 앞의 판들은 자료가 어디서 왔는지를 등급 줄에만 적었다.
이 판은 **줄마다 적는다.** 두 사람이 그 과의 녹음을 열어 그 자리를 들을 수 있다.

지어낸 것이 아니라는 말을 글로 적는 것과 **찾아갈 수 있게 하는 것**이 다르다.

## 21. 두 쪽을 다 보는 검사 (T299)

못 알아들은 척 판정 검사다. 판정 묶음 119~128 이다.

앞의 열세 판은 **한쪽만 봐도 됐다.** 답이 새는 쪽이 하나였기 때문이다.
이 판은 감출 것이 하나씩 양쪽에 있어 (T298) 두 쪽을 다 본다.

| 무엇 | 어느 쪽 | 어떻게 |
|---|---|---|
| 대본 줄 | 되묻는 쪽에 없다 | `innerHTML` 에 그 줄이 한 글자도 없다 |
| 되묻기 세기 | 뭉개는 쪽에 없다 | **거울 판의 자를 든다.** 세기를 고정해 두 번 그려 견준다 |

두 자가 다르다. 줄은 **있나 없나**로 재고 세기는 **달라지나 안 달라지나**로 잰다.
줄은 긴 글자라 있으면 보이고 세기는 이름이 짧아 class 하나로도 샌다.

**무엇을 감추느냐가 어느 자를 들지를 정한다.**

### 21.1 보기가 없는 단에 다른 단 보기가 뜨면 더 나쁘다

빈 자리에 지어낸 줄을 넣는 것보다 **다른 단의 줄을 넣는 것이 더 나쁘다.**
지어낸 것은 두 사람이 모르는 것이고 다른 단의 줄은 **틀린 것을 배우는 것**이다.

그래서 그 자리를 따로 잰다. 모든 단의 보기를 모아 놓고
빈 단 화면에 그중 하나라도 있으면 실패다.

### 21.2 이 검사의 첫 실패도 검사 탓이었다

셈을 재는 자리에서 이렇게 짰다.

```js
await tap(kMud, "#rskAlone", "보기 없이");
kMud = (누가 뭉개는 쪽인가 다시 본다);   // <- 여기가 틀렸다
await tap(kMud, "#rskShown", "보기 보고");
```

누르고 나면 이 기기가 되묻는 쪽이 된다. 그때 **다른 기기를 봐도 소용없다.**
그쪽은 회가 안 밀렸다. 회는 기기마다 자기가 센다 (`round.md` 6장).

한 줄 더 밀어 이 기기를 다시 뭉개는 쪽으로 만들었다.

**다섯 번째다** (T239, T249, T253, T281, T296 에 이어).
새 검사가 처음 내는 실패는 검사 자신을 먼저 의심한다.

### 21.3 여덟을 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 줄을 두 화면에 다 그린다 | 잡힘 |
| 세기를 뭉개는 쪽에도 그린다 | 잡힘 |
| 열기 전에도 보기를 그린다 | 잡힘 (셋) |
| 보기가 없는 단에 다른 단 보기를 낸다 | 잡힘 (셋) |
| 다음 줄에서 보기를 안 닫는다 | 잡힘 |
| 판정 단추를 되묻는 쪽에도 둔다 | 잡힘 (셋) |
| 보기에서 과 이름을 뺀다 | 잡힘 |
| 마감에서 보기가 벌이 아니라는 말을 뺀다 | 잡힘 (둘) |

## 22. 시계를 한 기기만 드는 판 (T301)

끼어들기다. 열다섯째 판이고 스무 판 중 제일 짧다 (3분).

신호 시각은 두 기기가 같은 벌을 본다 (T300). **그런데 시계가 어긋난다.**
두 사람이 시작을 각자 누르면 몇 초씩 벌어지고 (`round.md` 5장)
같은 표를 봐도 다른 순간에 울린다.

**한쪽만 울리면 그 사람만 준비를 하고 그러면 압박이 없어진다.**

그래서 시계를 한 기기만 든다. 어느 기기인지는 **회 0의 읽는 쪽**이고
두 기기가 그 값을 셈으로 얻는다. 마주 앉아 있으니 소리는 둘 다 듣는다.

파장에서 시계를 한 화면에만 둔 것과 같은 자리다 (T292).
다만 이유가 다르다. 파장은 **판정이 한쪽에 있어서**고 이 판은 **소리가 하나여야 해서**다.

### 22.1 못 한 것을 못 한 채로 넘긴다

규칙서의 못 했을 때 칸이 셋을 만든다.

| 무엇 | 역할 | 셈 |
|---|---|---|
| 끼어들었다 | 뒤집힌다 | **든다** |
| 한 번 못 했다 | 그대로 | 안 든다. **신호가 한 번 더 온다** |
| 두 번째도 못 했다 | 뒤집힌다 | **안 든다** |

셋째가 이 판의 자리다. **역할은 바뀌는데 셈은 안 는다.**
못 한 것을 못 한 채로 넘긴다. 벌이 아니고 셈도 아니다.

앞의 판들은 못 했을 때 다시 하거나(파장) 미루거나(3초 벽) 접었다(이어달리기).
이 판은 **그냥 넘어간다.** 끼어드는 것이 편치 않은 일이라 그렇다.

### 22.2 상태를 시계 값으로 읽지 않는다

신호가 나 있는가를 처음에는 `CUTCLK.at>0 && !CUTCLK.t` 로 읽었다.
울린 신호가 있고 시계가 멈춰 있으면 신호 상태라는 셈이다.

**한 번 못 했을 때 그 셈이 무너진다.** 둘째 신호를 주려고 시계를 되돌리면
그다음 신호 자리가 앞으로 밀려 표와 어긋난다. 안 되돌리면 상태를 못 가린다.

`CUT.sig` 를 따로 뒀다. **다른 것에서 읽어 낸 상태는 그 다른 것이 바뀌면 무너진다.**
브라우저로 몰아 보고 알았다. 단추가 통째로 사라졌다.

### 22.3 셈이 절반도 같은 수도 아니다

시계를 든 기기만 센다. 다른 기기는 그 수를 **받아 적는다.**

| 법 | 어느 판 |
|---|---|
| 더한다 | 거울, 한 줄 바꾸기, 내 소리는 네가, 둘이 한 문장, 3초 벽, 한 사람만 본다, 파장, 못 알아들은 척 |
| 같은 수다 | 이어달리기, 누구 말이야 |
| 큰 것을 든다 | 되받아치기 |
| **한 기기가 센다** | **끼어들기** |

넷째가 여기서 처음 나왔다. 앞의 셋은 두 기기가 다 셌다.
이 판은 **셀 수 있는 기기가 하나다.** 신호를 그 기기만 알기 때문이다.

## 23. 자료와 화면을 한 검사가 같이 재는 판 (T302)

끼어들기 판정 검사다. 판정 묶음 129~138 이다.

앞의 열네 판은 검사가 **화면만** 봤다. 자료가 맞는지는 파생기가 봤다.
이 판은 다르다. **신호 사이가 규격 안인가**를 화면 검사가 다시 잰다.

```js
for (let i = 1; i < cspec.deck.length; i++) {
  const g = cspec.deck[i] - cspec.deck[i - 1];
  if (g < cspec.gap || g > cspec.far) no("신호 사이가 " + g + "초다");
}
```

파생기도 그것을 본다. 두 자리에서 같은 것을 재는 것이 낭비가 아니다.
**파생기는 만들 때 보고 이 검사는 화면이 그 벌을 그대로 쓰는지 본다.**
화면이 벌을 손대면 파생기는 아무 말도 안 한다. 깨 보니 그랬다.

### 23.1 시계가 한 기기에만 있는지를 잰다

| 무엇 | 어떻게 |
|---|---|
| 두 기기가 같은 벌 | `cutDeck()` 을 견준다 |
| 시계 단추 | 한 기기에만 있다. 둘 다 있으면 실패 |
| 회를 미는 단추 | 시계를 안 든 기기에만 있다 |
| 읽을 거리 | **둘 다 본다.** 감출 것이 없는 판이다 |

넷째가 앞의 판들과 반대다. 앞의 판들은 한쪽에만 있는지를 쟀고
이 판은 **둘 다 있는지**를 잰다.

### 23.2 못 한 것을 세 갈래로 잰다

| 누른 것 | 회 | 뒤집힘 | 그냥 바꿈 |
|---|---|---|---|
| 끼어들었다 | +1 | **+1** | 0 |
| 한 번 못 했다 | **그대로** | 0 | 0 |
| 두 번째도 못 했다 | +1 | 0 | **+1** |

가운데를 안 재면 "못 했다" 가 한 가지가 된다.
**규칙서가 셋으로 갈라 놓은 것을 검사가 둘로 합치면 그 사이가 안 잡힌다.**
파장에서 폭을 셋으로 잰 것과 같은 자리다 (T293).

그리고 **시계를 안 되돌렸는지**도 같이 잰다.
되돌리면 그다음 신호 자리가 앞으로 밀려 표와 어긋난다.
화면에는 안 보이는 자리라 `CUTCLK.at` 을 직접 본다.

### 23.3 여덟을 깨서 확인했다

| 깬 것 | 잡혔나 |
|---|---|
| 시계를 두 기기에 다 둔다 | 잡힘 (셋) |
| 신호가 나도 시계를 안 멈춘다 | 잡힘 (셋) |
| 한 번 못 해도 바로 넘긴다 | 잡힘 (셋) |
| 두 번 다 못 한 것도 뒤집힘으로 센다 | 잡힘 (둘) |
| 둘째 신호를 주려고 신호 자리를 되돌린다 | 잡힘 |
| 다른 기기가 셈을 건드린다 | 잡힘 |
| 마감에 `playHalf` 를 쓴다 | 잡힘 (둘) |
| 화면이 신호 벌을 손댄다 | **잡힘.** 파생기는 아무 말도 안 했다 |

## 24. 역할이 없는데 몫은 갈린다 (T304)

말 겹치기다. 열여섯째 판이다.

규칙서가 "역할은 둘 다 같다. **역할이 없다**" 라고 적었다.
자리가 안 돌고 판정도 둘이 같이 한다. **그런데 각자 다른 줄을 든다.**

**역할과 몫이 다르다.** 역할은 누가 판정하느냐이고 몫은 누가 무엇을 드느냐다.
겹치면 지운다도 그랬다 (T276). 자리는 없고 각자 적을 것이 있었다.

| | 역할 | 몫 |
|---|---|---|
| 무엇 | 누가 판정하나 | 누가 무엇을 드나 |
| 도나 | 판마다 다르다 | **이 판은 안 돈다** |
| 무엇으로 | `roundFirst` | `devicePerson` |

이 판은 `deviceSide` 를 안 쓴다. 그것은 날마다 뒤집힌다 (`roleOf`).
자리가 안 도는 판에서 몫만 도는 것은 규칙서에 없는 일이다.
`devicePerson` 이 그대로 몫을 정한다. **사람1이 늘 앞줄이다.**

### 24.1 자기 줄만 본다

상대 줄이 보이면 언제 끝날지를 눈으로 알고 그러면 겹치는 것을 피하게 된다.
**이 판은 일부러 겹치는 판이다.** 피할 수 있으면 안 겹친다.

가리는 이유가 앞의 판들과 반대다. 앞의 판들은 **답을 알면 안 되어서** 가렸다.
이 판은 **알면 안 하게 되어서** 가린다.

### 24.2 신호는 한 기기가 낸다

둘이 같은 순간에 시작해야 한다. 두 기기가 각자 세면 어긋난다 (`round.md` 5장).
끼어들기에서 정한 것과 같다 (T301). **사람1의 기기가 센다.**

다만 끼어들기와 다르다. 거기는 **언제 날지 모르는 것**이 판의 뼈대라
시계를 통째로 한쪽이 들었다. 여기는 셋을 세는 것뿐이라 시계는 둘 다 든다.

### 24.3 멈춘 것도 한 가지 답이다

못 했을 때 칸이 그렇게 적었다. 둘 다 멈추면 다시 시작한다.

**셈에 안 들 뿐이고 틀린 것이 아니다.** 회는 넘어간다.
"다시 시작한다" 는 그 회를 다시 하라는 말이 아니라 **다음 회로 간다**는 말이다.
넉 회를 도는 판에서 한 회를 무르면 끝 조건이 안 선다.

모자란 자료를 다루는 법이 넷이 됐다.
있는 만큼 돌기, 안 열기, 없다고 보여 주기, **셈에 안 넣고 넘기기**다.
