신뢰도: A 생성 (제작 관리)
분기: 전체
상위 규격: docs/spec.md
검증대상:
검증로그: 2026-08-08 / 개정 결정 13건과 제작물 반영 상태 대조 (T156 마감) / 통과 / 열세 건 중 아홉이 제작물과 검사기에 반영되어 있다. 5번과 11번은 기준서가 바뀌어야 반영되고 그때까지 어긋난 채로 둔다. 이 문서는 기준서 본문에 옮길 문안만 담는다
검증로그: 2026-08-08 / B단계에서 나온 기준서 충돌 다섯 (T207 마감) / 통과 / 14~18 을 더했다. 열여덟 건이다. 15번과 17번은 E단계 전에 정해져야 한다

# 기준서 개정문

`docs/spec.md` 는 사용자만 수정한다. 제작자는 못 고친다.
그래서 결정한 내용을 여기 문안으로 적어 둔다. 그대로 붙이면 된다.

결정 근거는 `state/journal.md` 5장에 있다.
**열여덟 건이다.** T207 에 B단계에서 나온 다섯을 더했다 (14~18).
앞의 열셋 중 아홉은 제작물과 검사기에 이미 반영되어 있고 기준서 본문만 옛 값이다.
둘(12번 13번)은 기준서가 자기 규칙에 걸리는 것이라 붙여야 검사가 깨끗해진다.

남은 둘은 기준서가 바뀌어야 반영할 수 있는 것이다.

- **5번 블록 비중**: 제작물이 51퍼센트고 검사기가 45로 잰다. Q1 경고 11건이 계속 난다.
  기준서를 고치거나 96편을 다시 쓰거나 둘 중 하나다. **고치는 쪽을 제안한다**
- **11번 역할 교대**: 앱과 자료가 기준서대로 날짜를 쓴다. 바뀌면 그때 고친다

**번호 9가 두 번 있었다.** 하나는 개정이 아니라 붙이는 순서 안내였다.
그것을 번호에서 빼고 문서 끝으로 옮겼다. T149 에서 고쳤다.

### 새 다섯은 갈래가 다르다 (T207)

| 번호 | 무엇 | 왜 지금 나왔나 |
|---|---|---|
| 14 | 8.1 이 Q1 압박 제한시간을 안 정한다 | 카드를 세어 보고 알았다 |
| 15 | 2.3 에 놀이가 들어갈 자리가 없다 | 판을 스무 개 적고 나서 알았다 |
| 16 | 13.2 개인 점수 금지의 범위 | 소리 기록과 부딪쳤다 |
| 17 | 놀이 판정과 트랙 통과 판정 | 열아홉을 사람이 판정한다 |
| 18 | 앱이 두 사람의 실력을 모른다 | 원칙 3과 V3 가 부딪쳤다 |

**앞의 열셋은 기준서가 옛 값이라 생긴 것이고 새 다섯은 기준서에 없던 자리다.**
놀이를 설계하기 전에는 물을 일이 없던 것들이다.
다섯 다 안 붙어도 지금 것이 돌아간다. 다만 E단계를 시작하기 전에 15번과 17번은 정해져야 한다.

### 열세 건이 세 갈래다 (T156 마감)

| 갈래 | 번호 | 지금 상태 |
|---|---|---|
| 제작물은 새 값, 기준서만 옛 값 | 1 2 3 4 6 7 8 9 10 | 아홉 건. 붙이면 맞는다. 안 붙여도 쓰는 데 지장 없다 |
| 기준서가 바뀌어야 제작물이 따라간다 | 5 11 | 두 건. 안 붙는 동안은 지금 값이 맞다 |
| 기준서가 자기 규칙에 걸린다 | 12 13 | 두 건. 붙여야 전수 검사가 깨끗해진다 |

**세 갈래는 서로 다른 물건이다.** 첫째는 미뤄도 되고 둘째는 정해야 하고 셋째는 고쳐야 한다.

이 개수와 번호는 `scripts/check_spec.py` 가 센다.
번호가 끊기거나 머리말의 개수가 안 맞으면 실패로 난다.
T149 에 열한 건을 여덟 건이라 적고 번호 9가 두 번 있던 일이 다시 안 나게 한 것이다.
**받는 사람이 제일 먼저 보는 줄이라 손으로 세지 않는다.**

---

## 1. 1장. 총 시간

**고칠 곳**: 총 730시간

**새 문안**

```
총 576시간. 하루 2시간, 주 6일, 48주에서 나오는 값이다.
730시간은 하루 2시간에 365일을 곱한 값이라 주 6일 설계와 안 맞는다.
```

---

## 2. 2.2 분기별 통과 조건

**고칠 곳**: 누적 열의 180h / 365h / 550h / 730h

**새 문안**

| 분기 | 누적 | 통과 조건 |
|---|---|---|
| Q1 | 144h | 축약 20종 청취 식별 90%, 강세 박자 재현, 되묻기 3형 자동 |
| Q2 | 288h | 고빈도 600청크 0.5초 인출, 90초 무중단, 자기수정 자연화 |
| Q3 | 432h | 3초 내 반응, LRE 세션당 8회 이상, 겹침 상황 처리 |
| Q4 | 576h | 3분 발화, 3레지스터 구분, 대화 주도권 전환 |

누적 값은 주 6일 두 시간에 12주를 곱한 것이다. 선언이 아니라 파생이다.

---

## 3. 4.7 영역 재등장

**고칠 곳**: "도입 후 최소 4회 재등장"

**새 문안**

```
각 영역은 전 과정에서 최소 5회 등장한다. 도입 1회에 재등장 4회다.
4.5 사다리는 1회차부터 4회차까지의 추상도를 정한다.
5회차 이상은 4단계 추상도를 유지한다.
```

---

## 4. 7.2 블록 분량

**고칠 곳**: 남는 800자를 블록 1과 2에 배분한다는 문장

**새 문안**

```
블록별 자수는 참고값이다. 규정은 7.4의 분기별 비중이다.
둘이 어긋나면 7.4를 따른다.
```

7.2의 절대 자수를 그대로 지키면 어느 분기든 블록1과 2가 63퍼센트가 된다.
7.4는 Q1 45에서 Q4 28로 줄이라고 한다. 둘을 다 지킬 수 없다.

---

## 5. 7.4 블록 비중

**고칠 곳**: Q1 45퍼센트

**새 문안**

| 분기 | 블록1+2 | 블록3+4 |
|---|---|---|
| Q1 | 51% | 34% |
| Q2 | 40% | 45% |
| Q3 | 33% | 52% |
| Q4 | 28% | 57% |

Q1 값을 45에서 51로 올린다. 스물네 편을 만들어 보고 나온 실측이다.
제로 시작 구간은 설명이 더 든다. 45는 그 구간을 안 만들어 본 상태에서 잡은 값이다.

**이 항목만 다른 일곱과 성격이 다르다.**
나머지는 모호한 것을 정한 것이고 이것은 목표값을 실측에 맞춰 옮기는 것이다.
그래서 `scripts/check_blocks.py` 는 안 고쳤다. 45로 계속 재고 Q1 경고 11건이 계속 난다.
기준서를 이 문안으로 고치기 전까지 그 경고는 맞는 경고다.

---

## 6. 9장. 음성 자료

**더할 곳**: 9장 끝

**새 문안**

```
음성 자료의 세부 규격은 docs/audio_intake.md 를 따른다.
C등급을 C-real 과 C-gen 으로 가르고, C-gen 은 1층 전용이며
소리 트랙 통과 판정에 쓰지 않는다는 조항이 그 문서에 있다.
기기에 내장된 음성 낭독도 C-gen 으로 본다.
```

---

## 7. 10.2 화자 수

**고칠 곳**: 분기별 화자 수 조건

**새 문안**

| 분기 | 화자 수 |
|---|---|
| Q1 | 1인 또는 2인 |
| Q2 | 2인 |
| Q3 | 3인 |
| Q4 | 4인 이상 |

Q1의 "1~2인"이 Q2의 "2인"을 삼켜서 Q2에 갈 자료가 논리적으로 없어진다.
`scripts/derive_speakers.py` 가 2인 이하를 레슨 순서로 갈라 Q1과 Q2에 나눈다.
VOA Level 1은 레슨 순서가 곧 난이도 순이라 앞쪽을 Q1으로 둔다.

---

## 8. 15장. 파일 배치

**더할 곳**: 파일명 표

**새 문안**

```
out/lectures/   본 강의
out/cards/      드릴 카드와 배정표
out/sets/       대조 교차 세트
out/dialog/     대화 자료 3층
out/input/      입력 조준표
out/tasks/      산출 과제집
out/emergency/  비상판
out/manual/     운영 매뉴얼, 진행 대장, 분기 점검 보고서
out/audio/      음성 대본. 음성 파일은 넣지 않는다
state/          진행 상태, 제작 일지, 회전 대장, 검증 대기열
docs/           기준서와 부속 규격
scripts/        검사기
```

회전 대장은 상태 파일이므로 `state/` 에 둔다.
11.4의 매뉴얼류 목표 3은 2로 내리고 분기 점검 보고서를 별도 항목 4로 뺀다.

---

## 9. 입력 조준표 길이 하한 (Q4에서 개정)

기준서 10.2가 재료 길이를 정한다. Q1부터 Q3까지 하한이 3분이었다.

**Q4 조준표에서 2분으로 내렸다.** 붙일 문안은 이렇다.

```
재료 길이는 2분에서 5분으로 한다.
분기별로 다르게 잡지 않는다.
```

이유는 하나다. Q4 재료 16과 중 8과가 1분 59초다.
그 한 과를 빼면 조건은 지켜지는데 화자가 여섯이라 재료 값이 크다.
78강이 그 과를 쓴다. 화자 여섯짜리가 Q4 재료에 둘뿐이다.

**Q3에서는 반대로 했다.** 짧은 과 셋을 조건을 못 지키는 채로 그냥 썼다.
그때는 3인 조건이 길이보다 먼저라고 적고 넘어갔다.

이번에 조건 자체를 고친 이유가 있다. **세 과가 어긋나면 재료 문제이고
한 과가 어긋나면 조건 문제일 수 있다.** 그 자리에서 3분 하한의 근거를 다시 봤다.

3분이라는 값에 근거가 없었다. 2분이면 3분 발화의 형태를 볼 수 있다.
Q1 재료에는 30초짜리도 있었다. 하한을 분기마다 올린 적도 없다.

이 개정이 붙으면 Q1부터 Q3까지의 조준표도 같은 하한을 쓰게 된다.
**Q3 조준표 2장에 적어 둔 짧은 과 셋에 대한 변명이 필요 없어진다.**
그 세 과가 2분 조건 아래에서는 조건 위반이 아니다.

## 10. 비상판 총량 60에서 80으로 (T82에서 개정)

기준서 11.2가 비상판을 60개로 고정한다. 붙일 문안은 이렇다.

```
80개. 분기마다 20개. 각 15분. 1인 수행 가능. 이 과정 유일한 1인 예외.
```

**60개는 Q1과 Q2와 Q3에 스무 개씩이다. Q4에 하나도 없다.**
T82에서 세어 보고 처음 알았다. 세 파일이 있고 셋 다 Q1 Q2 Q3 이었다.

Q3 파일 끝에 "060 까지 다 썼으면 Q2 것을 쓴다" 는 문장이 있다.
그 문장이 있어서 규격 위반은 아니다. 앞 분기 것을 다시 도는 것이 허용된다.

그런데 그 규칙에는 다른 값이 딸려 있다.
비상판은 인출 장치다. 배운 것을 꺼내는 것만 한다.
Q4를 도는 열두 주에 상대가 없는 날이 오면 Q4 재료를 한 번도 안 꺼낸다.
**제일 최근에 배운 것이 인출에서 제일 먼저 빠지는 구조다.**

Q4 재료가 다른 분기와 다르기도 하다.
Q1은 소리 열다섯 편이고 Q4는 화용 여덟 편이다.
소리는 앞 분기 것을 다시 돌아도 같은 것을 꺼내는 것이 된다.
화용은 그렇지 않다. 협상과 3분 발화는 Q4에만 있다.

**분기마다 스무 개면 규칙이 하나로 선다.** 60이라는 값에는 근거가 없었다.
20 곱하기 3이 아니라 20 곱하기 분기 수다. 분기가 넷이므로 80이다.

이 개정을 전제로 061부터 080까지를 만들었다.
개정이 안 붙으면 그 스무 개는 규격 밖 산출물로 남는다.
그 상태에서도 쓰는 데는 지장이 없다. 60개 규칙을 깨는 것이 아니라 넘는 것이다.

**넷을 뺐다.** 83강과 94강과 95강과 96강이다.
83강 주도권은 상대가 있어야 생기고 95강은 여덟 편을 겹쳐 도는 자리다.
94강과 96강은 측정이다. 문항을 모르는 상태여야 값이 나온다.
Q3에서 069부터 072까지를 뺀 것과 같은 기준이다.

## 11. 역할 교대 기준을 날짜에서 세션 번호로 (T106에서 발견)

기준서 4장이 이렇게 정한다.

```
A와 B는 매일 교대한다. 짝수 날 남편 = A, 홀수 날 아내 = A.
```

붙일 문안은 이렇다.

```
A와 B는 세션마다 교대한다. 홀수 세션 남편 = A, 짝수 세션 아내 = A.
세션 번호는 진행 대장의 정상 수행 횟수다.
```

**날짜로 정하면 결석했을 때 역할이 안 바뀐다.**
2일과 4일에 하고 3일에 쉬면 두 날 다 짝수라 남편이 연달아 A다.
사흘 쉬면 같은 사람이 세 번 연달아 A가 되는 날도 나온다.

**하루도 안 빠져도 안 바뀌는 날이 있다.** T124 에서 앱으로 1년치 날짜를 돌려 세 봤다.

```
2026-09-01  2026-11-01  2027-01-01  2027-02-01  2027-04-01  2027-06-01  2027-08-01
```

**365일 중 이레다.** 31일로 끝나는 달의 다음 1일이다. 31일도 홀수고 1일도 홀수다.
그날은 아내가 이틀 연달아 A다. 결석과 무관하고 개근해도 그렇다.

### 이레가 아니라 마흔여덟이었다 (T346에서 다시 셌다)

위의 이레는 **달력 날을 이어 세어 나온 값**이다. 그런데 세션은 일요일을 건너뛴다.

    토요일 다음 세션은 월요일이다. 날짜가 둘 뛴다.
    **둘 뛰면 짝홀이 그대로다.**

그래서 달이 안 바뀌는 주는 토요일과 월요일의 A가 같은 사람이다.
1년 288세션을 앱의 `roleOf` 로 돌려 세니 **잇달아 같은 자리가 마흔여덟 번**이고
그중 마흔다섯이 토요일에서 월요일로 넘어가는 자리다.

    48주 중 마흔다섯 주가 그렇다. 거의 매주다.

이레는 예외처럼 보이고 마흔여덟은 규칙처럼 보인다. **같은 결함인데 크기가 일곱 배다.**
T124 가 작게 센 까닭은 하나다. **쉬는 날을 안 뺐다.**
`scripts/check_role.js` 가 그 값을 재서 박아 둔다. 이 개정이 붙으면 0이 된다.

한 사람에게 몰리는 정도도 같이 쟀다. 288세션에서 142 대 146 이고
시작일을 1년치로 바꿔 가며 본 최악이 10회 차이다. **그것은 크지 않다.**
문제는 몰리는 것이 아니라 **주마다 한 번씩 교대가 멈추는 것**이다.

그러니 기준서 4장의 두 문장이 서로 안 맞는다.
"매일 교대한다" 가 규칙이고 "짝수 날 남편 = A" 가 그 규칙의 구현인데
구현이 규칙을 1년에 일곱 번 어긴다. **둘 중 하나는 틀린 문장이다.**
세션 번호로 바꾸면 두 문장이 같은 말이 된다.

역할 교대의 목적은 기준서 4장이 적은 대로 관계 유형 고착 방지다.
**고착은 결석이 잦을 때 제일 잘 생긴다.** 그때 교대가 멈추는 것이 이 규칙의 구멍이다.

세션 번호로 정하면 쉰 날이 몇이든 한 세션마다 정확히 바뀐다.
세션 번호는 이미 앱이 진도를 재는 값이라 새로 셀 것이 없다.

**날짜 규칙의 장점 하나는 잃는다.** 달력만 보면 안다는 것이다.
그래서 앱이 그 값을 화면에 띄운다. 종이만 쓰는 날에는 진행 대장의 수행 칸을 센다.
비상판을 쓴 날은 세션 번호가 안 오르므로 역할도 안 바뀐다.
그날은 혼자 하는 날이라 역할이 없다. 어긋나지 않는다.

이 개정이 붙기 전까지 앱과 자료는 기준서대로 날짜를 쓴다.
세트 파일 48개가 "짝수 세트는 남편이 A" 로 적혀 있던 것을 T106 에서 날짜로 고쳤다.
**기준서와 어긋난 문장을 자료가 들고 있었다.** 그것은 개정과 무관하게 잘못이다.

---

## 12. 기준서 자신이 문자 범위 규칙에 걸린다 (T151에서 발견)

기준서 18장이 공통 점검 항목으로 쓰는 문자 범위를 정한다.
검사기가 그것을 구현하고 있다. **그 검사기를 기준서에 대면 기준서가 실패한다.**

129째 줄부터 132째 줄까지 네 줄이다. 블록 이름 앞에 **동그라미 친 숫자** 하나씩이다.
1부터 4까지 넷이고 유니코드 자리는 U+2460 부터 U+2463 이다.

**그 글자를 여기 그대로 옮겨 적지 않는다.** 적으면 이 문서가 같은 검사에 걸린다.
검사기는 글자를 보지 뜻을 안 본다. T149 에 제작 일지에서 같은 일이 있었다.

붙일 문안은 이렇다. 동그라미 숫자를 보통 숫자로 바꾸는 것뿐이다. 동그라미 숫자를 보통 숫자로 바꾸는 것뿐이다.

```
| 1 병렬 침묵 | 40분 | 같은 공간, 각자 헤드폰 | 금지 |
| 2 대조 교차 | 30분 | A 설명 → B 재구성 | 필수 |
| 3 페어 드릴 | 30분 | A 출제 → B 반응, 시간 압박 | 필수 |
| 4 공동 입력 | 20분 | 동일 자료 함께 청취 | 부분 |
```

동그라미 숫자는 기기마다 다르게 보이거나 안 보인다.
글꼴에 그 글자가 없으면 네모로 나온다. 종이로 뽑을 때도 같다.
**블록 번호는 두 사람이 매일 소리 내어 부르는 이름이다.** 안 보이면 못 부른다.

제작물 900편은 이미 보통 숫자를 쓴다. 기준서만 다르다.
**이 개정은 뜻을 안 바꾼다.** 표기만 바꾼다.

---

## 13. 기준서에 신뢰도 등급 표시가 없다 (T151에서 발견)

기준서 18장이 전 제작물에 신뢰도 등급 표시를 요구한다.
**요구한 문서 자신에는 없다.** 제목 줄 다음에 한 줄 넣으면 된다.

붙일 문안은 이렇다. 3째 줄 앞에 넣는다.

```
신뢰도: A 생성 (설계 기준)
```

이 문서는 근거를 인용하는 문서가 아니라 규칙을 정하는 문서다.
그래서 A다. **A가 "맞다" 는 뜻이 아니라 "내가 정한 것" 이라는 뜻이다.**
등급은 출처의 종류를 적는 칸이지 품질 등급이 아니다.

두 건을 한 줄로 요약하면 이렇다.
**규칙을 만든 문서가 그 규칙 밖에 서 있었다.** T149 에 내 문서들에서 같은 것을 고쳤다.
남은 것이 기준서였고 그것은 내가 못 고친다. 그래서 문안만 여기 적는다.

---

## 14. 8.1 이 Q1 압박형 제한시간을 안 정한다 (T205에서 발견)

8.1 이 압박형 제한시간을 숫자로 정한다. **Q2 5초, Q3 3초, Q4 2초다.**
**Q1 이 없다.**

카드를 열어 보니 Q1 압박형 25장에 값이 붙어 있다. 8초 열둘, 10초 여덟, 3초 넷, 2초 하나다.
강의가 정한 것이다. 기준서가 안 정해서 아래에서 정했다.

붙일 문안은 이렇다. 8.1 의 제한시간 줄에 Q1 을 넣는다.

```
압박형 제한시간: Q1 8초, Q2 5초, Q3 3초, Q4 2초.
강의가 그보다 낮은 값을 그 강에 한해 정할 수 있다. 높이지는 못한다.
```

낮추는 것만 열어 두는 이유가 있다. **높이면 압박이 아니다.**
Q1 에 10초짜리가 여덟 장 있는데 그것이 8초 위다. 그 여덟은 강의가 다시 봐야 한다.

이 개정이 붙으면 검사가 하나 생긴다. 카드의 초가 그 분기 상한 안인지 보는 검사다.
**지금은 아무도 안 본다.**

---

## 15. 2.3 표준 2시간에 놀이가 들어갈 자리가 없다 (T204에서 발견)

2.3 이 표준 2시간을 블록 넷으로 나눈다. 40 + 30 + 30 + 20 이고 합이 120분이다.
**꽉 차 있다.**

로드맵 12.9 는 놀이 스무 판을 "블록 사이와 블록 안에 얹는다" 고 적었다.
얹을 자리가 없다. 2시간이 120분이고 블록이 120분이다.

풀리는 자리는 하나다. **놀이가 시간을 더 쓰는 것이 아니라 블록 안의 활동을 대신한다.**
블록 3 페어 드릴 30분 중 5분을 3초 벽 판으로 도는 것이 그것이다.
같은 훈련을 다른 꼴로 하는 것이지 훈련을 미는 것이 아니다.

붙일 문안은 이렇다. 2.3 끝에 두 줄을 넣는다.

```
놀이는 블록 시간을 늘리지 않는다. 블록 안의 활동을 그 시간만큼 대신한다.
하루에 도는 놀이는 두 판까지다. 그 밖에 세션이 끝난 뒤 오늘의 한 판을 한 번 연다.
```

하루 두 판인 이유가 있다. 판이 5분이니 두 판이면 10분이다.
블록 하나에서 10분을 빼면 그 블록이 3분의 1이 줄어든다. **셋을 빼면 블록이 무너진다.**

스무 판 90분은 하루 합이 아니라 **스무 판 전체의 합**이다.
그것을 안 적어 두면 하루에 90분을 놀이로 쓰는 것으로 읽힌다.

---

## 16. 13.2 개인 점수 금지의 범위 (T200에서 발견)

13.2 가 "말수 적으면 손해" 전제를 막는다. 개인 점수를 기각한 근거가 이것이다.

그런데 채용한 것 중에 개인 값을 쌓는 것이 하나 있다. **처음 녹음과 오늘 녹음을 견주는 것**이다.
자기 소리는 자기 것이라 개인 값일 수밖에 없다.

둘이 부딪치는 것처럼 보이지만 아니다. 13.2 가 막는 것은 **값이 아니라 둘 사이의 우열**이다.

붙일 문안은 이렇다. 13.2 끝에 넣는다.

```
개인 값을 쌓는 것 자체는 막지 않는다. 막는 것은 두 사람의 값을 견주는 것이다.
소리 기록처럼 각자의 입에서 나온 것은 각자 쌓되 한 화면에 나란히 놓지 않는다.
```

이것이 없으면 소리 훈련을 못 한다. **처음과 지금을 못 견주면 는 것이 안 보인다.**
그리고 이것이 있으면 놀이 점수는 여전히 공동으로만 쌓인다. 둘이 갈린다.

---

## 17. 놀이 판정과 트랙 통과 판정을 가른다 (T206에서 발견)

9장이 Q1 소리 트랙 통과 판정을 C-real 로만 하라고 정한다.
기계 음성으로 통과를 매기지 말라는 뜻이다.

놀이 스무 판 중 열아홉을 **사람이 판정한다.** 마주 앉은 상대다.
그러면 그 판정이 트랙 통과 판정인가.

**아니다.** 그런데 어디에도 아니라고 적혀 있지 않다.

붙일 문안은 이렇다. 9장 끝에 넣는다.

```
놀이에서 상대가 내는 판정은 연습 판정이다. 트랙 통과 판정이 아니다.
트랙 통과는 강의가 정한 통과 기준과 그 회차의 자료로만 판정한다.
```

안 갈라 두면 두 가지가 무너진다.
**하나.** 상대가 통과를 매기면 그 판정이 쌓이고 그것이 개인 점수가 된다.
**둘.** 통과 기준이 판마다 달라진다. 5분짜리 놀이가 12주 트랙의 문턱이 된다.

놀이는 연습이고 통과는 강의가 매긴다. **그 둘을 한 자리에 두면 놀이가 시험이 된다.**

---

## 18. 앱이 두 사람의 실력을 모른다 (T201에서 결정)

2.4 가 지배-수동형 고착을 막으라고 요구한다. 대응 셋이 적혀 있다.
놀이 원칙 3이 그것을 받는다. **못 하는 쪽이 정보를 쥔다.**

그런데 앱이 그것을 하려면 누가 못 하는지를 알아야 한다.
알려면 사람별로 쌓아야 하고 그것이 13.2 가 막는 것이다.

**그래서 앱이 그 일을 안 하기로 했다.** 앱은 역할을 번갈아 돌리기만 하고
두 사람이 그 자리에서 뒤집는다.

붙일 문안은 이렇다. 2.4 대응 목록에 넣는다.

```
역할 배정은 도구가 번갈아 정하고 두 사람이 그 자리에서 바꾼다.
도구는 두 사람의 실력을 나타내는 값을 갖지 않는다. 누가 못 하는지는 두 사람이 안다.
```

이것이 더 나은 이유가 하나 더 있다. **실력은 트랙마다 다르다.**
소리는 한쪽이 낫고 청크는 다른 쪽이 나을 수 있다. 한 값으로 재면 그 차이가 뭉개진다.

그리고 이 개정은 검사를 하나 없앤다. 앱이 값을 안 가지므로 **볼 값이 없다.**
그 자리는 주 7일째 점검에서 두 사람이 센다.

---

## 붙이는 순서

**1번과 2번을 먼저 붙인다.** 나머지 열하나는 순서가 상관없다.

1번과 2번은 제작물에 이미 반영되어 있다.
72강 통과 기준과 36주차 마감 절차와 english.html 누적 곡선이 576 기준이다.
기준서만 아직 730이라 그 둘이 어긋나 있다. 붙이면 맞는다.

**5번은 반대다.** 제작물이 51로 되어 있고 검사기가 45로 잰다.
붙이면 Q1 경고 11건이 사라진다. 안 붙이면 그 경고가 계속 난다.
어느 쪽이든 제작물은 안 바뀐다. **그 경고는 안 붙은 동안에는 맞는 경고다.**

**11번은 붙는 순간 코드가 바뀐다.** 나머지 열 건과 다른 점이다.
앱의 `roleOf` 와 세트 48편의 역할 문장이 날짜에서 세션 번호로 간다.
붙이겠다고 정하면 그 작업이 한 턴 든다. 붙이기 전에는 지금이 맞다.

### 안 붙여도 되는 것과 안 되는 것

| | 안 붙이면 |
|---|---|
| 1 2 3 4 6 7 8 9 10 | 기준서만 옛 값이다. 제작물은 이미 새 값이라 쓰는 데 지장이 없다 |
| 5 | Q1 경고 11건이 계속 난다. 그 경고를 무시하는 습관이 생기는 것이 위험이다 |
| 11 | 1년에 이레는 같은 사람이 이틀 연달아 A다. 결석이 잦으면 더 자주 그렇다 |
| 12 | 블록 번호 넷이 기기에 따라 네모로 보인다. 뜻은 안 바뀐다 |
| 13 | 기준서가 검사기에 계속 걸린다. **전수 검사 결과에 실패 둘이 상수로 남는다** |
