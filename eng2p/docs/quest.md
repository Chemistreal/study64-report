# 공동 퀘스트. 한 주짜리 공동 목표

신뢰도: A 생성 (설계)
상위 규격: docs/spec.md 2.4 / docs/play.md 원칙 1 / docs/roadmap.md 12.15
작성일: 2026-08-10 (T324)

검증로그: 2026-08-10 / 주마다 셀 수 있는 값을 저장소와 대조했다 / 통과 / 셀 수 있는 여섯 중 둘이 못 쓰는 값이었다

## 1. 무엇을 하는 장치인가

한 주에 목표 하나를 준다. 주가 바뀌면 새것이 온다.
**둘이 같이 채운다. 개인 기여도는 안 보여 준다.**

로드맵 12.7 이 Duolingo 의 친구 퀘스트를 "채용하되 공동으로" 로 적었다.
거기서는 둘이 각자 얼마를 채웠는지가 보인다. **그것을 안 한다.**
보이면 그것이 곧 순위고 원칙 1을 어긴다.

## 2. 무엇을 셀 수 있나

퀘스트가 세는 값은 셋을 다 갖춰야 한다.

    공동이다      사람별로 안 갈린다
    건너간다      짝 코드로 합치면 두 기기가 같아진다
    저절로 는다    두 사람이 퀘스트를 위해 따로 할 일이 없다

셋째가 중요하다. 퀘스트를 채우려고 없던 일을 하게 만들면
그것은 세션을 밀어내는 것이고 원칙 6의 결을 어긴다.

`S.days[d]` 를 열어 여섯을 셌다.

| 값 | 공동 | 건너감 | 저절로 | 쓸 수 있나 |
|---|---|---|---|---|
| 마친 세션 수 (`status`) | O | O 그대로 온다 | O | **쓴다** |
| 발화 분 (`speak`) | O | O 큰 것을 남긴다 | O | **쓴다** |
| 카드 장수 (`cards`) | O | O 큰 것을 남긴다 | O | **쓴다** |
| 미해결 LRE (`lre`) | O | O 큰 것을 남긴다 | O | **쓴다** |
| 채집 표현 (`coll`) | O | O 모은다 | O | **쓴다** |
| 판을 몇 번 돌았나 (`rhit`) | 절반이다 | **X 안 건너간다** | O | **못 쓴다** |

### 2.1 판 셈을 퀘스트에 못 쓴다

`rhit` 는 짝 코드로 안 건너간다 (T320). 합치면 개인 칸이 생기기 때문이다.

그러면 기기 둘이 다른 수를 들고 **퀘스트가 기기마다 다르게 찬다.**
한쪽에서는 다 찼는데 다른 쪽에서는 안 찬 것으로 보인다.

판을 몇 개 돌았는지를 퀘스트에 넣고 싶었다. E단계에 스무 판을 만들었고
그것을 돌게 만드는 것이 F단계의 일이기 때문이다. **못 쓴다.**

대신 **오늘의 한 판을 열었는가**는 쓸 수 있다. 그것은 `rhit` 가 아니라
날 기록에 붙일 수 있는 값이다. T325 가 그 자리를 만든다.

**안 건너가는 값은 공동 목표가 못 된다.** 공동이라는 말이 두 가지를 뜻한다.
사람별로 안 갈린다는 뜻과 두 기기가 같은 것을 본다는 뜻이다. 둘 다여야 한다.

### 2.2 미해결 LRE 는 방향이 반대다

미해결이 늘면 좋은 것이 아니다. **적힌 것이 느는 것은 좋다.**
그 둘이 다르다. 적고 나서 푸는 것이 그다음 일이다.

그래서 이 값으로 목표를 세울 때는 "다섯 개를 적는다" 지 "다섯 개를 푼다" 가 아니다.
푸는 것은 판정 탭에서 하고 그것은 주 단위가 아니다.

## 3. 주가 무엇인가

**세션 주다. 달력 주가 아니다.** `plan().week` 이 그것이다.

밀리면 달력은 가고 세션 주는 안 간다. 퀘스트가 달력을 따라가면
밀린 두 사람에게 못 채울 목표가 계속 쌓인다. **밀린 것을 벌로 만들지 않는다.**

## 4. 무엇을 안 하나

| 안 하는 것 | 왜 |
|---|---|
| 개인 기여도 | 원칙 1. 그것이 곧 순위다 |
| 못 채운 주를 세어 보여 주기 | 원칙 4. 못한 것을 쌓아 보이면 벌이다 |
| 퀘스트로 잠그기 | 채워야 다음 주가 열리면 밀린 사람이 못 나간다 |
| 남은 시간 재촉 | 세션을 밀어낸다 |

**못 채워도 다음 주가 온다.** 채운 주가 몇인지는 세지만 못 채운 주는 안 센다.

## 5. 주마다 무엇을 주나 (T325~T327)

**아래 표가 원본이다.** `scripts/derive_quest.py` 가 이것을 읽어 자료로 만든다.

T325 에 다섯 주치로 화면과 검사를 세웠고 T326 이 Q1 과 Q2 를,
T327 이 Q3 와 Q4 를 채웠다. **마흔여덟 주가 다 찼다.**

### 5.0 분기마다 성격이 다르다

| 분기 | 주 | 무엇을 민다 | 왜 |
|---|---|---|---|
| Q1 | 1~12 | **앉는 것** | 양이 아니라 습관이다. 세션과 판을 여는 것 |
| Q2 | 13~24 | **말하는 양** | 90초 무중단이 Q2 통과 조건이다 |
| Q3 | 25~36 | **적는 것** | 세션당 LRE 8회가 Q3 통과 조건이다 |
| Q4 | 37~48 | **섞는 것** | 앞의 셋을 돌려 쓴다. 한 가지에 안 몰린다 |

성격을 분기가 정하고 **통과 조건이 성격을 정한다.**
퀘스트가 통과 조건과 다른 곳을 밀면 두 사람이 두 가지를 좇게 된다.

### 5.0.1 같은 갈래를 이틀 잇달아 안 준다

Q3 는 적는 것을 미는 분기인데 그렇다고 열두 주 내내 LRE 만 주면
**퀘스트가 배경이 된다.** 사이사이에 다른 갈래를 넣는다.
Q3 의 여섯 주가 LRE 고 나머지 여섯이 다른 것이다.

### 5.1 표

| 주 | 갈래 | 목표 | 이름 |
|---|---|---|---|
| 1 | session | 5 | 다섯 날을 채운다 |
| 2 | one | 3 | 오늘의 한 판을 사흘 연다 |
| 3 | cards | 60 | 카드 60장을 돈다 |
| 4 | session | 5 | 다섯 날을 채운다 |
| 5 | one | 4 | 오늘의 한 판을 나흘 연다 |
| 6 | coll | 3 | 들은 말 셋을 적는다 |
| 7 | session | 5 | 다섯 날을 채운다 |
| 8 | cards | 90 | 카드 90장을 돈다 |
| 9 | one | 4 | 오늘의 한 판을 나흘 연다 |
| 10 | speak | 60 | 둘이 60분을 말한다 |
| 11 | session | 5 | 다섯 날을 채운다 |
| 12 | one | 5 | 오늘의 한 판을 닷새 연다 |
| 13 | speak | 72 | 둘이 72분을 말한다 |
| 14 | cards | 120 | 카드 120장을 돈다 |
| 15 | speak | 84 | 둘이 84분을 말한다 |
| 16 | one | 5 | 오늘의 한 판을 닷새 연다 |
| 17 | speak | 96 | 둘이 96분을 말한다 |
| 18 | coll | 5 | 들은 말 다섯을 적는다 |
| 19 | speak | 108 | 둘이 108분을 말한다 |
| 20 | cards | 150 | 카드 150장을 돈다 |
| 21 | speak | 120 | 둘이 120분을 말한다 |
| 22 | session | 5 | 다섯 날을 채운다 |
| 23 | speak | 132 | 둘이 132분을 말한다 |
| 24 | one | 5 | 오늘의 한 판을 닷새 연다 |
| 25 | lre | 12 | 미해결을 열둘 적는다 |
| 26 | coll | 6 | 들은 말 여섯을 적는다 |
| 27 | lre | 18 | 미해결을 열여덟 적는다 |
| 28 | speak | 132 | 둘이 132분을 말한다 |
| 29 | lre | 24 | 미해결을 스물넷 적는다 |
| 30 | coll | 8 | 들은 말 여덟을 적는다 |
| 31 | lre | 30 | 미해결을 서른 적는다 |
| 32 | cards | 180 | 카드 180장을 돈다 |
| 33 | lre | 36 | 미해결을 서른여섯 적는다 |
| 34 | coll | 10 | 들은 말 열을 적는다 |
| 35 | lre | 42 | 미해결을 마흔둘 적는다 |
| 36 | one | 5 | 오늘의 한 판을 닷새 연다 |
| 37 | speak | 144 | 둘이 144분을 말한다 |
| 38 | lre | 48 | 미해결을 마흔여덟 적는다 |
| 39 | cards | 210 | 카드 210장을 돈다 |
| 40 | coll | 10 | 들은 말 열을 적는다 |
| 41 | speak | 156 | 둘이 156분을 말한다 |
| 42 | one | 5 | 오늘의 한 판을 닷새 연다 |
| 43 | lre | 48 | 미해결을 마흔여덟 적는다 |
| 44 | cards | 240 | 카드 240장을 돈다 |
| 45 | speak | 168 | 둘이 168분을 말한다 |
| 46 | coll | 12 | 들은 말 열둘을 적는다 |
| 47 | session | 5 | 다섯 날을 채운다 |
| 48 | speak | 180 | 둘이 180분을 말한다 |

### 5.1 갈래 여섯째를 만들었다

2장에 다섯을 세고 판 셈은 못 쓴다고 적었다. 그래서 **판을 세는 길을 하나 새로 냈다.**

`one` 은 그날 오늘의 한 판을 열었는가다. `rhit` 와 다르다.

| | `rhit` | `one` |
|---|---|---|
| 어디 사나 | `S.rhit` | **`S.days[d]`** |
| 무엇인가 | 판정 셈. 절반이다 | 열었는가 안 열었는가 |
| 건너가나 | **안 간다** (T320) | 간다. 큰 것을 남긴다 |

셈이 아니라 **열었는가**라 절반이 없다. 둘이 같이 여는 것이고 한쪽만 눌러도
합치면 온다. 그래서 이 값은 공동 목표가 된다.

**판을 돌게 만드는 길이 셈이 아니라 문이었다.**

### 5.1.1 Q3 에서 LRE 가 열둘에서 마흔여덟까지 간다 (T327)

Q3 통과 조건이 **세션당 LRE 8회 이상**이다 (매뉴얼 226줄). 주 엿새면 마흔여덟이다.

25주에 열둘로 시작해 35주에 마흔둘까지 간다. 한 주에 여섯씩 오른다.
**분기 끝에 통과 조건에 닿게 놓았다.** 처음부터 마흔여덟을 주면 못 채우고
못 채우는 주가 이어지면 두 사람이 이 칸을 안 본다.

38주와 43주에 마흔여덟이 두 번 나온다. Q4 는 지키는 분기다.
**오르는 것이 아니라 그 자리를 지키는 것이 Q4 의 일이다.**

### 5.1.2 Q4 는 앞의 셋을 돌려 쓴다

37주부터 48주까지 열두 주에 speak 넷, lre 둘, cards 둘, coll 둘, one 하나, session 하나다.

한 가지에 안 몰린다. **Q4 통과 조건이 "3분 발화, 3레지스터 구분, 대화 주도권 전환"
이라 어느 하나가 아니다.** 퀘스트도 어느 하나가 아니어야 한다.

speak 이 넷으로 제일 많다. 144에서 180까지 오른다.
그것이 Q4 의 통과 조건에 제일 가깝기 때문이다.

### 5.2 채운 것을 못 보여 주는 목표는 목표가 아니다

`session` 목표를 여섯으로 뒀다가 걸렸다.

`plan()` 은 **끝낸 세션 수**로 주를 센다. 여섯째를 마치는 순간 주가 넘어간다.
그러면 채우자마자 다음 주 퀘스트가 뜨고 **두 사람은 채웠다는 말을 한 번도 못 본다.**

    채운 것을 못 보여 주는 목표는 목표가 아니다.

그래서 `session` 과 `one` 의 윗선을 다섯으로 막았다. 오늘의 한 판도
세션이 있어야 열리므로 같은 자리다. `derive_quest.py` 가 그 선을 지킨다.

이 자리는 T316 에서 겪은 것과 같다. 거기서도 마치는 순간 `plan()` 이 내일을
가리켰다. **끝낸 것으로 세는 값은 끝나는 순간 다음을 가리킨다.**

### 5.3 목표를 못 채워도 다음 주가 온다

4장에 적은 대로다. 채운 주가 몇인지는 세고 **못 채운 주는 안 센다.**
화면이 남은 것을 재촉하지 않는다. 지금 얼마인지만 적는다.

## 6. 마흔여덟을 통째로 잰다 (T328)

주 하나씩 보면 다 맞는데 마흔여덟을 죽 늘어놓으면 틀린 것이 있다.
**한 줄씩 재는 것과 표를 재는 것이 다르다.**

| 무엇 | 왜 |
|---|---|
| 분기가 미는 갈래 | Q1 은 앉는 것, Q2 는 말, Q3 는 적기를 제일 많이 줘야 한다 |
| 한 갈래가 절반 넘게 | 넘으면 퀘스트가 배경이 된다 (5.0.1) |
| Q4 의 갈래 수 | 다섯은 돼야 한다. 앞의 셋을 돌려 쓰는 분기다 |
| 안 나오는 갈래 | 마흔여덟에 한 번도 안 나오면 그 갈래는 없는 것과 같다 |
| 뒤로 가며 내려가는 목표 | 쉬어 가는 주를 두려면 여기 적어야 한다 |

### 6.1 마지막 줄이 진짜를 잡았다

12주에 오늘의 한 판을 닷새 열고 **16주에 나흘로 내려가고 있었다.**

Q2 가 말을 미는 분기라 판을 낮춰도 된다고 생각하고 적은 것이다.
그런데 두 사람 눈에는 그냥 **줄어든 것**이다. 지난 주에 닷새를 열었는데
이번 주에 나흘을 열라고 하면 그것은 목표가 아니다.

닷새로 올렸다. 내리려면 **왜 내리는지를 적어야 한다.** 안 적으면 실수다.

### 6.2 깸 여덟 중 셋이 엉뚱한 줄에 잡혔다

Q2 의 갈래를 바꿔 봤더니 "같은 갈래가 잇달아 온다" 가 났다.
이웃 규칙이 먼저 걸린 것이고 **내가 겨눈 줄은 안 걸렸다.**

이웃을 안 건드리는 자리로 옮겨 다시 깨니 겨눈 줄이 잡혔다.
T314 에 적은 대로다. **잡혔는지만 보면 안 되고 어느 줄이 잡았는지를 본다.**
