# 마찰 측정 기록

신뢰도: A 생성 (측정 기록)
상위 규격: docs/roadmap.md 12.10 A단계
측정일: 2026-08-08 (T159, T160)

앱을 열고 무엇을 하기까지 몇 번 누르고 화면이 얼마나 긴지 잰 것이다.
**고치기 전에 잰다.** 안 재고 고치면 좋아졌는지 알 수 없고, 나중에 나빠져도 모른다.

7장의 표를 `scripts/check_friction.js` 가 읽어서 지킨다. **늘면 실패다.**

---

## 1. 재는 조건

| | |
|---|---|
| 화면 | 390 x 844 (손전화), 900 x 844 (책상) |
| 상태 | 오늘에서 138일을 거슬러 시작. 일요일 빼고 다 수행 |
| 그때 진도 | 20주 6일째, 120번째 세션 |
| 오늘 것 | 40강, 세트 Q2-120, 카드 092부터, 미디어 lle1-29, 비상판 36 |
| 브라우저 | Chromium. `file://` 로 연다 |

**날짜를 못 박지 않는다.** 오늘에서 거슬러 채운다.
못 박으면 내일 돌릴 때 진도가 하루씩 밀려 값이 흔들린다.
빈 상태로 재도 안 된다. 화면이 짧아서 실제와 다르다.

---

## 2. 세션 시작까지

| 길 | 누름 | 채울 칸 | 첫 그림 | 세션이 돌기까지 |
|---|---|---|---|---|
| 처음 여는 사람 | 2 | 2 (이름 둘. 날짜는 오늘이 기본) | 1017ms | 1622ms |
| 이튿날 | **1** | 0 | 516ms | 866ms |
| 끊긴 데서 이어 | **1** | 0 | 538ms | 895ms |

**내 진단이 틀렸다.**

로드맵에 "세션 시작까지 여러 번 누른다"고 적었다. 재 보니 **한 번이다.**
2차 로드맵 D구간에서 원탭 시작을 이미 넣었고 그것이 제대로 돌고 있다.
끊긴 세션도 "이어서 하기" 단추 한 번으로 **멈춘 그 블록으로** 돌아간다.

**여기는 고칠 것이 없다.** 지키기만 하면 된다.
그래서 이 셋이 검사의 첫 세 줄이 됐다.

시간은 검사에 안 건다. 기기와 그날 상태에 따라 흔들린다.
시간은 K단계의 `check_perf.py` 가 따로 맡는다.

---

## 3. 화면의 부피

T159 에 잰 값과 T166 에 고친 뒤의 값을 나란히 둔다.

| | T159 | T166 뒤 |
|---|---|---|
| 오늘 화면 높이 (390px) | 4231px | **2508px** |
| 화면 몇 개분 | 5.0 | **3.0** |
| 누를 것 전체 | 77개 | **27개** |
| 미디어 탭 높이 | 21540px (25.5화면) | 그대로. K단계에서 본다 |

무엇이 자리를 먹고 있었는지 칸마다 재 봤다.

| 칸 | 높이 | 세션이 돌 때만 쓰는가 |
|---|---|---|
| 시계 묶음 | 1626px | **예** |
| 조작줄 | 98px | **예** |
| 오늘 한 장 | 545px | 아니오 |
| 머리와 큰 단추 | 134px | 아니오 |

**시계 묶음과 조작줄이 1724px 이다. 전체의 41% 다.**

### 주 단추 규격 (T172)

| | |
|---|---|
| 높이 | 48px 이상 |
| 글자 | 14px 이상 (좁은 칸 안이면 13px) |
| 굵기 | 650 이상 |
| 오늘 화면 | **제일 큰 단추는 세션 시작이어야 한다** |

44px 은 손가락이 겨우 닿는 값이지 **흘끗 보고 누르는 값이 아니다.**
책상에 놓고 마주 앉아 쓰는 물건이다.

재다가 둘이 나왔다.

**하나.** 미디어 탭의 "음성 듣기" 가 굵기 400 으로 그려지고 있었다.
`.mitem .macts button{font:inherit}` 이 뒤에 와서 주 단추의 굵기를 지웠다.
**`font:inherit` 는 굵기까지 되돌린다.** 그 탭의 주 행동인데 옆 단추와 똑같아 보였다.

**둘.** 오늘 화면에서 제일 큰 단추가 세션 시작(308x55)이 아니라
세트 칸(270x68)이었다. **눈이 큰 것을 먼저 본다.** 앉자마자 세트를 누르게 된다.
세션 시작을 62px 로 키웠다.

### 한 손으로 닿는가 (T171)

| | T171 전 | T171 뒤 |
|---|---|---|
| 엄지 밖에 있는 탭 | **6개** | 0개 |
| 44px 못 채우는 누를 자리 | 1개 | 0개 |

844px 짜리 화면에서 엄지가 닿는 자리는 아래 3분의 2다.
탭 여섯이 다 화면 위쪽 64px 에 있었다.
**제일 자주 누르는 것이 제일 안 닿는 자리에 있었다.**

손전화 너비에서 탭을 아래로 내렸다. 세션 중에는 조작줄이 그 자리를 쓰므로 접는다.
그리고 아래 띠가 마지막 줄을 덮지 않는지도 같이 잰다.
**띄워 놓고 자리를 안 비우면 제일 아래 단추를 영영 못 누른다.**
그리고 그것은 세션이 돌 때만 쓴다. 시작 전에는 큰 단추 하나만 있으면 된다.
그래서 시작 전에는 접는다. 중간에 멈춘 상태면 접지 않는다.
그때는 어디서 멈췄는지가 제일 중요하기 때문이다.

**접는 것보다 펴는 것이 더 중요하다.** 접기만 하고 안 펴지면 세션을 못 돈다.
그래서 검사에 줄을 둘 더 넣었다. 세션을 시작했을 때와 끊긴 데서 이어 갈 때
시계가 실제로 펴지는지를 본다.

---

## 4. 탭

| | 개수 | 누름 |
|---|---|---|
| 겉에 보이는 탭 | 5 | 1번 |
| 더보기 안 | **7** | **2번** |

겉: 오늘 복습 소리 클립 미디어
안: 자료 대장 판정 분기 검사 회전 규칙

**자료가 안에 있다.** 강의록과 세트가 거기 있다. 매일 쓰는 것이 두 번 눌러야 나온다.
대장 판정 분기 검사 회전은 하루에 한 번도 안 여는 것들이라 안에 있는 것이 맞다.

---

## 5. 오늘 것에 닿기까지

**이것이 T159 에서 나온 제일 큰 것이다.**

오늘 칸은 여섯 가지를 알려 준다.

```
40강
Q2 · 소리 트랙 · 20주 6일째 · 오늘이 120번째 세션이다
세트 Q2-120 / 카드 092부터 / 미디어 lle1-29 / 과제 250자 / 비상판 36
```

그 칸 안에서 누를 수 있는 것이 **0개다.**

| 오늘 것 | 앱이 데려다주는가 | 사람이 하는 일 |
|---|---|---|
| 40강 본문 | 아니오 | 자료 탭(2번) 열고 96편에서 찾는다 |
| 세트 Q2-120 | 아니오 | 자료 탭에서 288편 중 찾는다 |
| 카드 092 | 아니오 | 복습 탭에서 600장 중 찾는다 |
| 미디어 lle1-29 | 아니오 | 미디어 탭에서 52개 중 찾는다. 그 탭이 25.5화면이다 |
| 과제 250자 | 해당 없음 | 숫자다 |
| 비상판 36 | 예 | 오늘 화면에서 펴진다 |

**여섯 중 하나만 닿는다.**

미디어 탭에는 검색칸이 있으니 `lle1-29` 를 쳐서 갈 수는 있다.
그런데 그것은 앱이 이미 아는 값이다.
**앱이 오늘 무엇인지 알면서 사람에게 그것을 찾아오라고 시키고 있다.**

이것은 누름 횟수의 문제가 아니다. 누름은 두세 번이다.
문제는 **화면이 알려 준 것과 화면이 열어 주는 것이 이어져 있지 않다는 것**이다.
두 사람이 앉아서 "40강이 어디 있지"를 매일 한다.
하루 이 분이면 1년에 열여섯 시간이다.

---

## 6. 측정이 계획을 고쳤다

로드맵 12.10 을 이 결과로 다시 잡았다.

| 원래 계획 | 측정 결과 | 바뀐 것 |
|---|---|---|
| 세션 시작을 한 번 누름으로 (T168) | **이미 한 번이다** | 지키는 검사로 바꿨다 |
| 이어 하기 (T169) | **이미 된다** | 지키는 검사로 바꿨다 |
| 첫 화면 새로 (T166) | 5화면, 누를 것 77개 | 그대로 간다. 더 급해졌다 |
| (계획에 없었다) | **오늘 것 여섯 중 하나만 닿는다** | **T168 T169 로 새로 넣었다** |

**안 재고 계획대로 갔으면** 이미 되어 있는 것에 두 턴을 쓰고,
매일 걸리는 자리는 계획에 아예 없는 채로 지나갔을 것이다.

G구간에서 배운 것과 같은 모양이다. 적어 둔 것은 검사가 아니다.
**이번에는 계획이 그랬다. 계획도 검사가 아니다.**

---

## 6b. A단계 마감 (T187)

T159 에 재고 T187 에 다시 쟀다. **같은 잣대로 같은 조건에서 잰 값이다.**

| | T159 | T187 | |
|---|---|---|---|
| 오늘 화면 높이 | 4231px | **2499px** | 5.0화면에서 3.0화면으로 |
| 오늘 화면의 누를 것 | 77개 | **25개** | 유령 일곱을 걷어낸 것 포함 |
| 오늘 것 중 눌러서 닿는 것 | 1 / 6 | **5 / 6** | 과제 분량은 숫자라 갈 데가 없다 |
| 엄지 밖에 있는 탭 | 6개 | **0개** | 손전화에서 탭이 아래로 |
| 44px 못 채우는 누를 자리 | 1개 | **0개** | |
| 열자마자 읽는 것 | 546KB | **410KB** | 접힌 칸이 읽던 136KB |
| 대비 선 아래 | 안 쟀다 | **0곳** | 화면 16개 2152곳 |
| 되돌릴 수 있는 자리 | 5 | **13** | 덮어쓰는 자리 여덟이 늘었다 |
| 세션 시작까지 누름 | 1 | 1 | **고칠 것이 없던 자리다** |

마지막 줄이 이 표에서 제일 중요하다.
**계획서에는 그 자리를 고치라고 적혀 있었다.** 재 보니 이미 되어 있었다.
그 두 턴을 오늘 칸을 누를 수 있게 만드는 데 옮겼고 그것이 A단계에서 제일 큰 것이 됐다.

### 안 고친 것

| | 왜 |
|---|---|
| 미디어 탭 높이 21540px | 52과가 다 펼쳐진다. 검색칸이 있다. K단계에서 본다 |
| `english.html` 298KB | 주석 53KB 를 빼면 40ms 를 번다. 안 바꾼다 (8장) |
| 첫 화면을 다시 손댈 것 | 놀이와 공동 연속일이 F단계에 들어온다. 자리는 비워 뒀다 |

---

## 7. 기준선

`scripts/check_friction.js` 가 이 표를 읽어서 지킨다.
**검사가 여기서 값을 읽는다.** 두 자리에 적으면 언젠가 갈라지고
그때 어느 쪽이 맞는지 알 수 없게 된다. 그래서 이 표가 하나뿐인 자리다.

| 이름 | 기준선 | 방향 | 무엇인가 |
|---|---|---|---|
| `tap_day2` | 1 | 늘면 실패 | 이튿날 세션 시작까지 누름 |
| `tap_resume` | 1 | 늘면 실패 | 끊긴 데서 이어까지 누름 |
| `tap_first` | 2 | 늘면 실패 | 처음 여는 사람이 세션까지 누름 |
| `today_px` | 2838 | 늘면 실패 | 오늘 화면 높이 (390px) |
| `today_taps` | 27 | 늘면 실패 | 오늘 화면의 누를 것 |
| `tabs_open` | 6 | 늘면 실패 | 겉에 보이는 탭 |
| `today_reach` | 5 | **줄면 실패** | 오늘 것 여섯 중 눌러서 닿는 것 |

`today_px` 만 백에 셋을 봐준다. 글꼴 그리기에 따라 화소가 조금 흔들린다.
나머지는 세는 값이라 그대로 견준다.

마지막 줄은 방향이 반대다. **오늘 것 여섯 중 눌러서 닿는 것이 지금 하나다.**
T168 과 T169 가 이 값을 올린다. 올린 뒤에는 내려가면 실패다.

### 올린 기록

**나빠졌는데 올린 것이 아니라 새로 넣은 것 때문에 올렸다.** 아래에 이유를 적는다.

| 날 | 값 | 왜 |
|---|---|---|
| T166 | 4231 → 2508 | 안 시작한 세션의 시계를 접었다. 좋아져서 내렸다 |
| T167 | 2508 → **2562** | 고리 셋을 넣었다. 아래 참조 |
| T168 | 2562 → **2574** | 오늘 칸 넷이 단추가 됐다. 테두리와 여백이 12px |
| T168 | 누를 것 27 → **31** | 같은 자리다. 아래 참조 |

| T171 | 2574 → **2492** | 탭을 아래로 내리고 닫힌 메뉴를 자리에서 뺐다 |
| T171 | 누를 것 31 → **24** | 닫힌 메뉴 단추 일곱이 세어지고 있었다 |
| T172 | 2492 → **2499** | 세션 시작을 55px 에서 62px 로 키웠다 |
| T179 | 누를 것 24 → **25** | 48주 띠가 단추가 됐다. 눌러서 길 지도를 연다 |
| T210 | `eager_kb` 410 → **415** | 조준표를 블록 칸에 폈다. 아래 참조 |
| T211 | `eager_kb` 415 → **420** | 적는 칸을 만들었다. **두 턴에 두 번 올렸다.** 아래 참조 |
| T212 | `eager_kb` 420 → **425** | 3단계에 목록과 적는 칸. **천장까지 5KB 남았다** |
| T213 | `eager_kb` 425 → **380** | 차림표를 늦게 읽는다. **좋아져서 내렸다** |
| T213 | `eager_files` 3 → **2** | 같은 자리다 |
| T213 | 천장 430 → **400** | 여유가 53KB 면 천장이 아니다 |
| T322 | 2499 → **2632** | 공동 연속일 칸이 들어왔다. **T166 이 비워 둔 자리다.** 아래 참조 |
| T322 | `eager_kb` 298 → **300** | 같은 자리. 연속일 셈과 화면 2KB |
| T323 | `eager_kb` 300 → **302** | 회복권. 셈과 대장 칸 2KB. **천장까지 8KB** |
| T324 | `eager_kb` 302 → **303** | 퀘스트가 세는 함수 둘. **천장까지 7KB** |
| T325 | `eager_kb` 303 → **304** | 퀘스트 한 줄. **천장까지 6KB.** 아래 참조 |
| T331 뒤 | `eager_kb` 304 → **289** | 클립 탭을 늦게 읽는다. **좋아져서 내렸다** |
| T331 뒤 | 천장 310 → **300** | 여유가 22KB 면 천장이 아니다 |
| T332 | `eager_kb` 289 → **291** | 신호 셈과 처방 한 줄. 분기 탭에서 꺼내 왔다 |
| T334 | `eager_kb` 291 → **292** | 되돌아보기 칸과 그 선. **그리는 코드는 늦게 읽는다.** 천장까지 8KB |
| T335 | 2632 → **2743** | 기준서 12장 구간 줄. **스물한 주만 뜬다.** 아래 참조 |
| T335 | `eager_kb` 292 → **293** | 같은 자리. 셈과 줄 1KB. 천장까지 7KB |
| T336 | `eager_kb` 293 → **287** | 주간 점검을 늦게 읽는다. **좋아져서 내렸다** |
| T338 | `eager_kb` 287 → **288** | 누적 시간을 앱이 센다. `passVal` 1KB. 천장까지 12KB |
| T340 | `eager_kb` 288 → **289** | 합치기 통째 고르기와 글 잇기 1KB. 천장까지 11KB |
| T344 | `eager_kb` 289 → **290** | 트랙 진도 칸 껍데기 1KB. **그리는 코드는 늦게 읽는다** |
| T348~T349 | `eager_kb` 290 → **291** | 판 머리의 자리 안내 두 줄. 천장까지 9KB |
| T351 | `eager_kb` 291 → **292** | 분기 점검 주 줄과 셈. 천장까지 8KB |
| T354 | 2743 → **2838**, `today_taps` 26 → **27** | 기기가 하나인 날 줄. 아래 참조 |
| T354 | `eager_kb` 292 → **293** | 같은 자리. 천장까지 7KB |
| T355 | `eager_kb` 293 → **294** | 어제 못 했다는 줄과 셈. 천장까지 6KB |
| T358 | `eager_kb` 294 → **295** | 카드 간격을 사람별로 가르는 셈. 천장까지 5KB |
| T359 | `eager_kb` 295 → **296** | 막힌 카드 셈과 비상판 줄. **천장까지 4KB** |
| T361 | `eager_kb` 296 → **281** | 자료 탭과 규칙 탭을 늦게 읽는다. **좋아져서 내렸다.** 아래 참조 |
| T371 | `eager_kb` 281 → **282** | 배속 사다리 칸 셋과 그 칸 기준 줄. 천장까지 18KB |
| T381 | `eager_kb` 282 → **283** | 진동 표 셋과 부르는 자리. 첫 화면 칸이라 늦게 못 읽는다. 천장까지 17KB |
| T383 | `eager_kb` 283 → **284** | 켠 것을 남기는 셈. 첫 화면 칸이라 늦게 못 읽는다. 천장까지 16KB |
| T384 | `eager_kb` 284 → **285** | 화면 켜 두기가 안 될 때의 말. 세션 자리라 늦게 못 읽는다. 천장까지 15KB |
| T215 | `eager_kb` 380 → **385** | 카드 제한시간 시계. 사람이 세던 것을 시계가 잰다 |
| T217~T218 | `eager_kb` 385 → **390** | 블록 3 구간 알림과 블록 4 두 자리 |
| T219~T221 | `eager_kb` 390 → **395** | 회차 되돌리기, 조준표 금지, 미리 읽기 |
| T222~T223 | `eager_kb` 395 → **400** | 시계 하나만 크게, 세션 중 짙은 판. **천장에 닿았다** |
| T224 | `eager_kb` 400 → **370** | `.js` 판에서 여백을 뺐다. **좋아져서 내렸다** |
| T224 | 천장 400 → **390** | 여유가 30KB 면 또 느슨하다 |
| T225~T226 | `eager_kb` 370 → **378** | 끊김 복구와 주간 점검 화면 |
| T227 | `eager_kb` 378 → **382** | 비상판 15분 시계 |
| T259 | `tabs_open` 5 → **6**, `today_taps` 25 → **26** | 판 탭. 아래 참조 |
| T260 뒤 | `eager_kb` 309 → **310** | `scope="col"` 50곳. PR #9. 아래 참조 |
| T268 | `eager_kb` 310 → **311** | 판 넷의 자리 규칙과 목록. 아래 참조 |
| T280 | `eager_kb` 311 → **312** | 판 여덟. 예고한 대로 판마다 조금씩 는다 |

T171 은 좋아져서 내린 쪽이다. 닫힌 메뉴가 `display:grid` 때문에 계속 자리를
차지하고 있었다. **눈에 안 보이는데 세어지고 있었다.**
T164 에 대비를 잴 때 그 단추들이 섞여 들어와 값이 이상하게 나온 것도 같은 원인이다.

T168 의 누를 것 넷은 **새로 생긴 것이 아니라 원래 읽던 칸이 눌리게 된 것이다.**

누를 것이 많은 것이 나쁜 이유는 고를 것이 많아서다. 그런데 이 넷은 고를 것을
늘린 것이 아니라 **찾아가는 일을 없앤 것이다.** 세트 번호를 읽고 자료 탭에 가서
288편에서 찾던 일이 한 번 누르는 일이 됐다.

**이 잣대의 한계다.** 세는 것으로는 그 둘이 안 갈린다.
`today_reach` 가 그것을 가르라고 있는 줄이다. 그 값이 1에서 5로 올랐다.
둘을 같이 보면 갈린다. 한 줄만 보면 안 갈린다.

T167 은 늘어난 쪽이다. 고리가 92px 을 먹는다. 대신 두 줄을 지웠다.
겹치는 숫자 줄과 역할 교대 안내다. 둘 다 고리와 바로 위 문장이 이미 말한다.
남는 것이 54px 이다.

**3% 여유 안이라 그대로 둘 수도 있었다. 안 그랬다.**
그 여유는 글꼴 그리기가 흔들리는 폭을 봐주려고 둔 것이지
늘어난 것을 숨기라고 둔 것이 아니다. 그것으로 덮으면 다음에 진짜로
54px 이 더 늘 때 검사가 못 잡는다. **여유를 쓰면 여유가 없어진다.**

### T210 에 열자마자 읽는 것을 5KB 올렸다

블록 1이 40분인데 화면이 "조준표 과제대로 듣는다" 고만 말하고 있었다.
어느 과제인지는 종이에 있었다. 그것을 블록 칸에 폈다.

| | |
|---|---|
| 는 것 | `english.html` 298KB → 304KB |
| 주는 것 | 40분짜리 블록에서 종이 한 편이 빠진다 |

**종이를 펴면 화면과 종이가 둘 다 켜진 상태가 된다.** 그 상태가 40분 간다.
5KB 는 넷 배 느린 기기에서 재 봐도 눈에 안 띈다. 여는 데 300ms 안팎이고 재는 판마다 흔들린다.
그래서 시간에는 선을 안 건다. 8장 첫 표가 그 규칙이다.

조준표 자료 자체(`input.js`)는 **열자마자 안 읽는다.** 블록 칸에 들어갈 때 읽는다.
파일 수 셋은 그대로다. 는 것은 앱 코드뿐이다.

### 두 턴에 두 번 올렸다 (T211)

T210 에 5KB, T211 에 4KB 를 올렸다. **이것이 바로 검사를 끄는 방식이다.**
한 번에 크게 올리면 눈에 띄는데 4KB 씩 올리면 안 띈다. 그리고 스무 턴이면 80KB 다.

C단계가 앞으로 스무 턴 더 남았고 그 턴들이 다 블록 화면을 만든다.
**지금 규칙을 안 박으면 C단계 끝에 500KB 가 된다.**

그래서 천장을 박는다.

| | |
|---|---|
| 천장 | **430KB** |
| 넘으면 | 올리지 않는다. **줄이는 쪽을 만든다** |

줄이는 쪽이 하나 있다. `media/english/catalog.js` 47KB 다.
지금 열자마자 읽는데 그 안에서 첫 그림에 쓰는 것은 **과 이름 한 줄**뿐이다.
그 한 줄을 `index.js` 에 넣으면 47KB 가 늦게 읽는 쪽으로 간다.

K단계(T428~T447)가 그 일을 받기로 했었다. **T212 에 천장까지 5KB 만 남아 앞당겼다.**
T213 에 했다. C단계가 만든 늘어남이니 C단계가 갚았다.

### T213 에 차림표를 늦게 읽는 쪽으로 옮겼다

첫 그림에 차림표를 쓰는 자리가 **하나도 없었다.**
과 이름을 찾는 자리가 넷인데 넷 다 누르고 나서 도는 자리다.
블록 칸도 세션에 들어가야 그린다. 그런데도 열 때마다 47KB 를 읽고 있었다.

| | 전 | 후 |
|---|---|---|
| 파일 | 3개 | **2개** |
| 바이트 | 422KB | **377KB** |

천장도 430에서 400으로 내렸다. **여유가 53KB 면 천장이 아니다.**
T223 에 천장에 닿아 T224 에 `index.js` 를 줄였다. **자르는 대신 여백을 뺐다.**
`.js` 는 브라우저가 읽고 `.json` 은 사람이 읽는다. 내용은 같다. 64KB 가 30KB 가 됐다.
같은 규칙이 파생 자료 전부에 걸려 `out/data` 가 4.6MB 에서 4.1MB 가 됐다.

다음에 천장에 닿으면 그때는 **48주 차림표를 이번 주 한 줄로 줄인다.** 그것이 남은 길이다.

기준선을 올릴 때 붙는 조건도 적어 둔다.
**블록에서 종이를 빼는 것이면 올린다.** 그 밖의 이유로는 안 올린다.
T210 과 T211 이 둘 다 그것이었다. 조준표와 적는 칸이다.

### 기준선을 고치는 법

**고쳐도 되는 경우는 하나뿐이다.** 좋아져서 값이 내려갔을 때다.
그때는 새 값으로 낮춰 박는다. 낮춰 박아야 다시 나빠지는 것이 잡힌다.

나빠졌는데 기준선을 올리는 것은 검사를 끄는 것과 같다.
그렇게 하려면 **왜 늘어야 했는지를 이 문서에 글로 적는다.**
G구간의 면제표와 같은 규칙이다. 문턱을 올리는 대신 이유를 적는다.

---

## 8. 여는 데 걸리는 것

`scripts/check_perf.js` 가 이 표를 읽어서 지킨다.

**잣대를 둘로 나눈다.**

| | |
|---|---|
| 처음에 읽는 바이트 | 기계와 상관없다. **여기에 선을 건다** |
| 여는 데 걸리는 시간 | 기계마다 다르다. 적어 두기만 한다 |

시간에 선을 걸면 그 선은 이 기계의 선이다. 다른 기계에서 돌리면 틀린 값이 된다.
바이트는 안 그렇다. 그래서 게이트는 바이트가 맡는다.

| 이름 | 기준선 | 방향 | 무엇인가 |
|---|---|---|---|
| `eager_kb` | 285 | 늘면 실패 | 열자마자 읽는 것의 합 |
| `eager_files` | 2 | 늘면 실패 | 열자마자 읽는 파일 수 |
| `eager_ceiling` | 300 | 기준선이 넘으면 실패 | **기준선을 올릴 수 있는 끝.** 아래 참조 |

`out/data` 가 4.8MB 다. 그중 열자마자 읽는 것은 차림표 하나뿐이어야 한다.

| 파일 | 크기 | 언제 읽는가 |
|---|---|---|
| `english.html` | 280KB | 열 때 (T235 에 주석을 뗐고 T313 뒤에 탭 여섯을 뺐다) |
| `eng2p/out/app/plays.js` | 157KB | **판 탭을 열 때** (T259). 판이 늘어도 여기서 는다 |
| `eng2p/out/app/late.js` | 73KB | **드물게 여는 탭 여덟을 열 때** (T313 뒤, T331 뒤, T361) |
| `eng2p/out/data/index_head.js` | 0.7KB | 열 때 (T245 에 분기 넷으로 쪼갰다) |
| `eng2p/out/data/index_q*.js` | 7.4KB씩 | **오늘 분기 하나만.** 지도와 카드가 나머지를 읽는다 |
| `media/english/catalog.js` | 47KB | **미디어 탭이나 그 과를 누를 때** (T213) |
| `lecturetext.js` | 786KB | 강 제목을 누를 때 |
| `cards.js` | 565KB | 블록 3에 갈 때 |
| `sets.js` | 493KB | 블록 2에 갈 때 |
| `lectures.js` | 348KB | 필요할 때 |
| `handouts.js` | 217KB | 필요할 때 |
| 나머지 여덟 | 405KB | 필요할 때 |

**무거운 다섯이 첫 그림에 끼면 그 자체로 실패다.**
검사가 붙는 script 를 세어 두고 열자마자 붙은 것이 있는지 본다.

### 접은 것이 안에서는 안 접혀 있었다 (T185)

처음 재 보니 열자마자 셋이 더 붙었다.

```
transcripts.js  112KB
cues.js          23KB
audiolen.js       1KB
```

원인은 T166 이었다. 세션 시작 전에 시계 묶음을 접었는데 **접은 것이 화면뿐이었다.**
블록 칸은 계속 만들어지고 있었고 만들면서 그 셋을 읽었다.
보이지도 않는 칸 때문에 열 때마다 136KB 를 더 읽었다.

**화면에 없는 것은 값이 없는 것이 아니다.**
접었으면 안에서도 안 만들게 고쳤다.

### 다시 열 때 새로 안 만든다 (T186)

미디어 탭은 52개 항목에 단추가 104개다. **블록 1은 매 세션 이 탭을 연다.**
목록이 그대로인데 매번 새로 만들고 있었다.

| 넷 배 느린 기기에서 탭 열기 | 처음 | 그 뒤 |
|---|---|---|
| 미디어 | 232ms | **113ms** |
| 자료 | 197ms | 88ms |
| 소리 | 114ms | 50ms |

무엇이 그려질지를 한 줄로 적어 두고 같으면 넘어간다.
블록 칸이 쓰는 것과 같은 수다.

**검사는 시간을 안 잰다.** 자리에 표시를 해 두고 다시 열었을 때 그 표시가
살아 있는지를 본다. 새로 만들었으면 표시가 사라진다. 기계와 상관없는 잣대다.

### 안 한 것과 그 이유

`english.html` 298KB 중 주석이 53KB(18%)다. 빼면 245KB 가 된다.
**안 뺐다.**

넷 배 느린 기기에서 40ms 쯤 번다. 그 대신 **이 파일 하나만 받아 간 사람은
왜 그렇게 만들었는지를 잃는다.** 저장소에는 `app/` 이 있지만 파일 하나만
저장하는 일은 실제로 일어난다. 40ms 와 그것을 바꾸지 않는다.

무거운 조각을 나중에 읽게 하는 것도 안 했다. 자바스크립트 214KB 가
스물한 조각에 고르게 퍼져 있어 하나를 빼도 얼마 안 준다.
운영 탭 다섯을 미루면 얼마쯤 벌지만 **`file://` 에서 조각이 늘면 실패할 자리가 는다.**

### 잰 시간 (선을 안 건다)

| 기기 | 여는 데 |
|---|---|
| 이 기계 그대로 | 95ms |
| 넷 배 느리게 | 343ms |
| 여덟 배 느리게 | 734ms |

여덟 배 느린 기기가 몇 해 된 손전화쯤이다. 그 기기에서도 1초 아래다.
**이 값이 나쁘지 않은 이유는 무거운 것을 안 읽기 때문이다.**
4.8MB 를 다 읽었으면 여덟 배 느린 기기에서 여러 초가 걸린다.

### 천장에 닿아서 갚았다 (T235)

T234 에 짝 코드를 넣었더니 처음에 읽는 것이 390KB 가 됐다. 천장이 390 이다.

천장은 **기준선을 올려 이 검사를 끄는 길**을 막으려고 둔 것이다.
닿으면 올릴 수가 없고 줄이는 쪽을 만들어야 한다. 그 자리에 왔다.

잰 것이 이렇다.

| | |
|---|---|
| 앱 전체 | 360.8KB |
| 그중 주석 | 104.3KB (28.9%) |

**주석을 파생물에서 뗀다.** 조각에는 그대로 남는다.
두 사람은 주석을 안 읽고 읽는 것은 나인데 나는 `app/` 을 읽는다.

떼는 규칙이 하나 있다. **줄에 걸린 것만 뗀다.**

    accept="audio/*,video/*"        HTML 속성 안에 /* 가 있다
    replace(/^[A-Z]\s*:\s*/,"")     정규식 리터럴 안에 */ 가 있다

아무 데서나 `/*` 를 찾으면 저 둘을 문다. 줄 첫머리에서 열고 줄 끝에서 닫는 것만
주석으로 본다. `check_app.py` 가 저 둘이 파생물에 남아 있는지를 본다.

| | 전 | 후 |
|---|---|---|
| `english.html` | 360.8KB | 273KB |
| 처음에 읽는 것 | 390KB | 303KB |
| 기준선 | 382 | 306 |
| 천장 | 390 | 340 |

**천장도 같이 내렸다.** 안 내리면 갚은 것이 다시 쓸 여유로만 남는다.
남은 여유가 34KB 다. 그 안에서 D단계부터 N단계까지 간다.

주석을 뗀 값은 검사 하나로 갚았다. `check_app.py` 가 이제 조각을 본다.
파생물을 보면 **주석이 검사 밖으로 나간다.** 그러면 그 검사가 조용한 것이
깨끗해서인지 안 봐서인지를 모른다.

### 갚은 것을 얼마나 빨리 태우나 (T237)

합치기 조각 하나가 9KB 를 먹었다. 기준선을 306에서 313으로 올렸다.
천장 340 아래라 올릴 수 있는 자리다. **그것이 이 장치가 시킨 대로다.**

그런데 속도를 적어 둔다.

| | |
|---|---|
| T235 에 갚은 것 | 88KB |
| 천장까지 남은 여유 | 34KB 였고 지금 13KB |
| 조각 하나가 먹은 것 | 9 / 3 / 3 / 4 / 3 / 0 / 3KB (T237~T243) |
| 남은 턴 | T244~T497. 254턴 |

**이 속도면 서너 조각 만에 천장에 닿는다.** 그때 또 갚아야 한다.

T238 은 3KB 였다. 조각을 새로 안 만들고 있던 조각을 고쳤기 때문이다.
**새 조각이 비싸고 고치는 것은 싸다.** D단계에 새 조각이 몇 개나 더 붙는지가 관건이다.

다음에 갚을 자리를 미리 적어 둔다. 닿고 나서 찾으면 그 턴이 통째로 간다.

| 갚을 자리 | 얼마쯤 | 왜 아직 안 했나 |
|---|---|---|
| `index.js` 30KB 를 주마다 쪼갠다 | 25KB쯤 | 48주 차림표를 다 읽어야 오늘 한 장이 그려진다 |
| 스타일에서 안 쓰는 자리 | 모름 | 재 본 적이 없다 |
| 탭 열둘 중 늦게 여는 것의 뼈대 | 모름 | 뼈대는 작고 글이 크다 |

**남은 13KB 로는 D단계도 못 넘긴다.** 다음 성능 실패는 기준선을 올려 넘기지 않고
아래 표의 첫째를 실제로 여는 턴이 된다. T244~T257 안에 그 턴이 온다.

### 그 턴이 T245 에 왔다. 그리고 고리는 없었다

적어 둔 대로 기준선을 안 올리고 차림표를 쪼갰다.

**"차림표를 쪼개려면 오늘이 몇 주인지를 알아야 하고 그것을 알려면 차림표가 있어야
한다" 고 적어 뒀었다. 틀렸다.** `plan()` 은 `S.days` 와 `S.start` 로 주차를 센다.
차림표를 한 줄도 안 본다. **고리를 확인 안 하고 적었고 그것이 한 턴을 미뤘다.**

| | 전 | 후 |
|---|---|---|
| 열자마자 | `index.js` 30KB 통째 | 머리 0.7KB + 오늘 분기 7.4KB |
| 처음에 읽는 것 | 329KB | 301KB |
| 기준선 | 327 | 303 |
| 천장 | 340 | **320** |

**천장을 다시 내렸다.** 갚은 26KB 중 17KB 만 다시 쓸 수 있게 뒀다.
안 내리면 갚은 것이 그대로 여유가 되고 다음 갚기가 또 밀린다.

남은 것도 적어 둔다.

| 갚을 자리 | 얼마쯤 | 왜 아직 안 했나 |
|---|---|---|
| 스타일에서 안 쓰는 자리 | 모름 | 재 본 적이 없다 |
| 탭 열둘 중 늦게 여는 것의 뼈대 | 모름 | 뼈대는 작고 글이 크다 |
| **판 스무 개** | 100KB쯤 | 아직 안 만들었다. 만들기 전에 자리를 정해야 한다 |

셋째가 예고다. E단계가 판 스무 개를 붙인다. 첫 판을 재 보고 정한다.

### 판을 붙이기 전에 자리를 정했다 (T259)

첫 판(거울)이 10KB 였다. 스물이면 100KB 고 천장 320 에 기준선 306 이면
**둘째 판에서 이미 넘는다.** 예고한 대로 붙이기 전에 자리를 정했다.

`app/play/` 를 english.html 에서 뗐다. `eng2p/out/app/plays.js` 로 나간다.
판 탭을 처음 열 때 읽는다.

| | 전 | 후 |
|---|---|---|
| 열자마자 | 316KB (판을 넣었을 때) | 309KB |
| 판 하나가 여는 데 더하는 것 | 10KB | **0** |
| 판 스물이 더할 것 | 100KB쯤 | 목록 한 줄씩 2KB쯤 |
| 기준선 | 306 | **309** |
| 천장 | 320 | 320 |

**천장은 안 내렸다.** 이번은 갚은 것이 아니라 안 쓰기로 한 것이다.
안 쓴 것으로 천장을 내리면 다음에 진짜로 갚을 때 내릴 자리가 없다.

기준선이 3KB 오른 것은 판 목록과 틀이다. 그것은 english.html 에 남는다.
목록이 없으면 판 탭이 무엇을 읽어야 하는지 모른다.

**블록 1은 40분 듣기고 판이 안 든다.** 그날 그 시간에 8KB 를 안 읽는다.
`out/data` 의 무거운 것을 미룬 것과 같은 자리다.

### 탭이 하나 늘었다 (T259)

E단계가 판 스무 개를 붙인다. 붙일 자리가 없었다.
겉에 보이는 탭이 다섯에서 여섯이 되고 오늘 화면의 누를 것이 스물다섯에서 스물여섯이 된다.
**늘어난 것이 딱 그 단추 하나다.** 두 잣대가 같은 것을 두 번 센 것이다.

올리기 전에 셋을 따져 봤다.

| 길 | 왜 안 했나 |
|---|---|
| 운영 메뉴 안에 넣는다 | `tabs_open` 은 그대로다. **그런데 세션 중에 한 번 더 눌러야 한다** |
| 오늘 탭 안에 넣는다 | `today_taps` 가 스무 개만큼 는다. 훨씬 나쁘다 |
| 조작줄에 단추를 넣는다 | 조작줄이 일곱이다. **여덟째는 좁은 화면에서 줄이 접힌다** |

첫째가 제일 아까웠다. 판은 **블록 안에서 도는 것**이라 세션 중에 연다.
그때 운영 메뉴를 펴고 고르는 것은 40분짜리 블록 안에서 두 번 누르는 일이다.
`today_reach` 가 재는 것이 그 거리고, 그 값을 지키려고 만든 잣대에
`tabs_open` 을 지키자고 반대로 가는 것은 앞뒤가 안 맞는다.

**이 자리는 임시다.** T318 이 스무 판을 블록 넷에 배치한다.
그때는 블록 칸에서 오늘 돌 판으로 바로 간다. 탭은 **목록을 보는 자리**로 남는다.
그 턴에 `today_reach` 가 오르는지를 같이 잰다. 오르면 이 한 칸은 값을 한 것이다.

### 여유 안에서 20px 이 숨었다 (T260)

밖에서 들어온 고침이 하나 있었다. PR #9 가 `english.html` 의
`<div class="brand">` 를 `<h1 class="brand">` 로 바꿨다. 낭독기가
무엇에 관한 화면인지를 읽게 하려는 것이고 **그것은 맞다.**

그런데 `h1` 에는 브라우저가 주는 위아래 여백이 붙는다.
글자 크기는 `.brand` 가 다시 정해서 안 흔들렸고 여백은 아무도 안 정했다.
**오늘 화면이 2499 에서 2519 로 자랐다.**

`today_px` 만 백에 셋을 봐준다. 20px 은 0.8% 라 검사가 "OK" 라고 적었다.

T167 에 이 자리를 이미 적어 뒀다.

> 3% 여유 안이라 그대로 둘 수도 있었다. 안 그랬다. 그 여유는 글꼴 그리기가
> 흔들리는 폭을 봐주려고 둔 것이지 늘어난 것을 숨기라고 둔 것이 아니다.

**적어 둔 것을 지키는 것은 사람이 하고 사람은 OK 를 안 읽는다.**
검사는 잰 값과 기준선을 나란히 적고 있었다. 숫자는 거기 있었다. 내가 안 봤다.

자를 조이지는 않았다. 조이면 글꼴 흔들림에 걸려 헛실패가 난다.
**대신 눈에 띄게 했다.** 여유 안에서 늘어난 줄에 이렇게 적는다.

```
OK   today_px      잰 값   2519 / 기준선   2499  **여유 안에서 20 늘었다.
                                       여유는 늘어난 것을 숨기라고 둔 것이 아니다**
```

`.brand` 에 `margin:0` 을 넣어 2499 로 되돌렸다.
보이는 것은 그대로고 낭독기만 달라진다. **바꾸려던 것만 바뀌었다.**

### 열세 주만 뜨는 줄을 기준선이 늘 들고 있다 (T335)

기준서 12장이 예고한 세 자리를 때 맞춰 띄우면서 `today_px` 가 2632 에서 2743 이 됐다.
한 줄인데 111px 이다. 390px 폭에서 세 줄로 접힌다.

이 줄은 48주 중 **스물한 주만 뜬다.** 8~15 와 17~29 가 그 주고
나머지 스물일곱 주에는 첫 화면에 이 줄이 아예 없다.

그런데 마찰 검사의 붙박이 자료가 138일치라 **20주에서 잰다.** 뜨는 주다.

    기준선이 제일 큰 상태를 재고 있다.

그쪽으로 둔다. 안 뜨는 주에서 재면 111px 이 검사가 못 보는 여유가 되고
그 여유에 다음 것이 들어온다. **제일 큰 상태를 재는 쪽이 안전하다.**

줄이려고 한 번 깎았다. 처음에는 구간 안에서도 왜 그런지를 날마다 적었고
그것이 넉 줄이었다. **예고할 때 한 번만 적기로 하고 한 줄을 뺐다** (66px).
남은 111px 은 이 줄이 있어야 하는 값이다.

### 첫 화면에 단추 하나를 더 뒀다 (T354)

매뉴얼 11장이 **없는 것이 상대인가 기기인가**를 가른다.
상대가 없으면 비상판 15분이고 기기가 하나면 두 시간을 다 돈다.
**둘을 헷갈리면 그날 두 시간을 통째로 버린다.**

앱은 상대가 없는 날만 첫 화면에서 한 번에 갔다.
기기가 하나인 날을 켜는 자리는 가르기 판 안의 단추 하나였다.

첫 화면 단추가 스물여섯에서 스물일곱이 됐다. **하나 늘었다.**
그 하나가 두 시간을 지킨다. 95px 도 같이 늘었다.

누를 것이 느는 것은 원래 나쁜 쪽이다. 그런데 이 단추는 **누르는 것이 아니라
가르는 것**이다. 안 누르는 날에는 글 한 줄이고 그 줄이 잘못 가는 것을 막는다.

### 같은 고침이 1KB 를 먹었다 (T260 뒤)

`scope="col"` 이 50곳이다. 한 곳에 열두 글자니 600바이트쯤이고
`noindex` 한 줄과 합쳐 열자마자 읽는 것이 309에서 310KB 가 됐다.

**갚을 자리를 찾지 않고 그냥 올렸다.** 이 1KB 는 내가 쓴 것이 아니라
낭독기가 표를 읽게 하는 값이다. 줄이려면 표 머리에서 `scope` 를 빼야 하고
그것은 화면 검수를 도로 빨간불로 만드는 일이다.

천장까지 10KB 가 남았다. **판 열아홉이 여기서 안 는다** (T259 에 뺐다).
다음에 무엇이 이 10KB 를 먹을지는 F단계에 가 봐야 안다.

### 판이 늘어도 안 는다고 적었는데 조금 늘었다 (T268)

T259 에 판 화면을 `english.html` 밖으로 빼면서 이렇게 적었다.

> 판 하나가 여는 데 더하는 것: **0**

**딱 맞는 말이 아니었다.** 뺀 것은 판을 그리는 코드고 두 가지가 남았다.

| 남은 것 | 판 하나에 |
|---|---|
| 판 목록의 한 줄 | 60바이트쯤 |
| **자리 규칙(CSS)** | 200~400바이트 |

뒤엣것을 안 셌다. 스타일 조각은 `english.html` 안에 있고 게으르게 못 읽는다.
첫 그림을 그릴 때 다 있어야 하기 때문이다.

판 넷에서 1KB 가 됐다. 남은 열여섯이면 4KB쯤 더 는다.
천장까지 9KB 가 남았으니 **E단계는 이대로 끝까지 간다.** 그 뒤가 문제다.

**T280 에 판 여덟이 되며 또 1KB 가 늘었다.** 네 판에 1KB 다. 예고한 속도 그대로다.
남은 열두 판이면 3KB 더 늘고 천장까지 8KB 가 남는다. 그 셈이 아직 맞다.

**"0" 이라고 적은 것이 "거의 0" 이었다.** 재 보기 전에는 그 둘이 같아 보인다.
천장이 그것을 네 판 만에 잡아냈다. 적어 둔 숫자를 검사가 다시 재는 자리가 이런 자리다.

**T285 에 판 열이 되며 또 1KB 가 늘었다 (312 -> 313).** 여덟에서 열까지 두 판에 1KB 다.
네 판에 1KB 라고 적었는데 이번은 두 판이었다. 3초 벽이 자리 규칙을 둘 새로 만들었고
(시계 칸과 단서 칸) 되받아치기는 이어달리기 것을 그대로 썼다.

**판마다 같은 값이 아니다.** 새 자리 규칙을 만드는 판이 비싸고 있는 것을 쓰는 판은 싸다.
T238 에 "새 조각이 비싸고 고치는 것은 싸다" 라고 적은 것과 같은 갈래다.

남은 열 판이 다 3초 벽 쪽이면 5KB 고 다 되받아치기 쪽이면 0이다.
천장까지 7KB 다. **아직 든다. 다만 셈이 판 수가 아니라 새 자리 규칙 수다.**

### 낭독기가 또 1KB 를 먹었다 (T290 뒤)

밖에서 PR #10 과 #11 이 들어와 입력칸 125개에 이름을 달았다.
`english.html` 안의 입력칸이 그중 여럿이고 313 이 314 가 됐다.

**T260 뒤와 같은 갈래다.** 그때는 `scope="col"` 50곳이 1KB 를 먹었다.
둘 다 내가 쓴 값이 아니라 **낭독기가 화면을 읽게 하는 값**이다.
줄이려면 이름을 떼야 하고 그것은 화면 검수를 도로 빨간불로 만드는 일이다.

갚을 자리를 안 찾고 그냥 올렸다. **천장까지 6KB 다.**

| 무엇이 먹었나 | 얼마 |
|---|---|
| 판 넷 (T268) | 1KB |
| `scope="col"` (T260 뒤) | 1KB |
| 판 넷 (T280) | 1KB |
| 판 둘 (T285) | 1KB |
| 입력칸 이름 (T290 뒤) | 1KB |

**접근성이 먹은 것이 둘이고 판이 먹은 것이 셋이다.**
남은 아홉 판이 다 새 자리 규칙을 만들면 2~3KB 고 그러면 천장까지 3KB 가 남는다.
F단계에 들어가기 전에 `index.js` 를 주마다 쪼개는 일이 온다. 8장 위 표의 첫째다.

### 판 셋에 또 1KB (T295)

열에서 열셋까지 1KB 다 (314 -> 315). 판 넷에 1KB 라던 셈으로 돌아왔다.
파장과 누구 말이야가 자리 규칙을 새로 만들었고 한 사람만 본다도 그랬다.

**천장까지 5KB 다.** 남은 일곱 판이 다 새 자리 규칙을 만들면 2KB 다.
그러면 F단계 앞에서 3KB 가 남는다.

이 셈이 맞으면 E단계는 넘긴다. **갚는 턴은 F단계 앞에 온다.**
위 표의 첫째(`index.js` 를 주마다 쪼갠다, 25KB쯤)가 그 자리다.
지금 여는 것이 아니라 **언제 열지를 적어 두는 것**이 이 표가 하는 일이다.

### 판 둘에 또 1KB (T301)

열셋에서 열다섯까지 1KB 다 (315 -> 316). 두 판에 1KB 로 다시 빨라졌다.
못 알아들은 척과 끼어들기가 자리 규칙을 하나씩 새로 만들었다.

**천장까지 4KB 다.** 남은 다섯 판이 다 새 자리 규칙을 만들면 1~2KB 다.
그러면 F단계 앞에서 2~3KB 가 남는다.

T295 에 "남은 일곱 판이 2KB" 라고 적었는데 두 판에 1KB 가 나갔다.
**셈이 판마다 다르다는 것을 알면서도 평균으로 적었다.**
남은 다섯은 세지 말고 **갚을 자리를 F단계 앞에 놓는 것**만 지킨다.

### 판이 아니라 나눠 쓰는 조각이 늘었다 (T310)

316에서 317이 됐다. **판 화면은 `english.html` 에 안 들어간다.**
이번에 는 것은 `app/js/24_round.js` 다. 열여덟 판이 다 쓰는 조각이다.

따로 쓰고 같이 펴기가 "시간이 되면 펴진다" 를 지켜야 했고
그 잠금을 푸는 갈래를 `revealGate` 에 붙였다. 문구가 하나 늘었다.

**나눠 쓰는 자리에 붙이는 것이 판에 붙이는 것보다 비싸다.**
판에 붙이면 `plays.js` 로 가고 그것은 판 탭을 열 때 읽는다.
`24_round.js` 에 붙이면 열자마자 읽는다.

그래도 거기에 붙였다. 세 판이 같은 단추를 쓰고 **한 벌만 두는 것이 T240 의 결정**이다.
스무 벌로 만들면 그중 하나가 안 잠근다.

**천장까지 3KB 다.** 남은 두 판(오늘의 한 판, 그리고 어제 그거)이 붙는다.
갚는 자리는 그대로 F단계 앞이다 (`index.js` 를 주마다 쪼갠다, 25KB쯤).

### 판이 아니라 옛 자리를 고치느라 늘었다 (T312)

317에서 318이 됐다. **이번에도 판 화면이 아니다.**
`06_cards.js` 에 돈 날 목록을 붙이고 `23_merge.js` 에 카드 합치기를 고쳤다.
둘 다 열자마자 읽는 조각이다.

기록을 고치는 일은 미룰 수 없다. **어제 그거 판이 그 기록으로 돌기 때문**이고
기록이 틀린 채로 판을 얹으면 판이 아니라 기록을 고쳐야 할 때 판까지 다시 짠다.

**천장까지 2KB 다.** 남은 판이 둘이고 갚는 자리는 그대로 F단계 앞이다.
2KB 안에 안 들면 **갚는 턴을 E단계 안으로 당긴다.** T315 앞에서 다시 잰다.

### 재 보니 천장까지 1.4KB 다. 갚는 턴을 당긴다 (T313)

318에서 319가 됐다. 어제 그거의 목록 한 줄 때문이다. 판 화면은 늦게 읽으니
`PLAYS` 에 붙는 한 줄만 열자마자 읽는다.

정확히 재면 **318.56KB** 이고 천장까지 **1.44KB** 다.

T312 에 "2KB 안에 안 들면 갚는 턴을 E단계 안으로 당긴다. T315 앞에서 다시 잰다" 고 적었다.
**그 자리에 왔다.** T315 까지 안 기다리고 T313 뒤에 갚는다.

갚을 자리를 다시 골랐다. 표의 첫째(`index.js` 를 주마다 쪼갠다)는 이미 끝났다
(T245 에 분기 넷으로 쪼갰다). 남은 큰 덩어리는 **드물게 여는 탭**이다.

| 탭 | 조각 | 언제 여나 |
|---|---|---|
| 검사 | `14_check.js` | 규격을 볼 때. 거의 안 연다 |
| 회전 | `15_rot.js` | 주에 한 번 |
| 분기 | `13_quarter.js` | 열두 주에 한 번 |
| 판정 | `12_verify.js` | 검증할 때 |

판 화면을 늦게 읽는 길이 이미 있다 (`plays.js`, T259). **같은 길을 한 번 더 쓴다.**

### 갚았다. 드물게 여는 탭 넷을 늦게 읽는다 (T313 뒤)

**318.56 에서 297.2KB 가 됐다.** 21.3KB 를 갚았다.
검사는 올림해서 재므로 기준선은 298이다.

| 탭 | 조각 | 뗀 크기 |
|---|---|---|
| 판정 | `late/12_verify.js` | 3.1KB |
| 분기 | `late/13_quarter.js` | 4.2KB |
| 검사 | `late/14_check.js` | 8.9KB |
| 회전 | `late/15_rot.js` | 5.2KB |

판 화면을 늦게 읽는 길이 이미 있었다 (`plays.js`, T259). **같은 길을 한 번 더 썼다.**
`app/order.txt` 의 접두사가 곧 묶음 이름이다. `play/` 는 `plays.js` 로,
`late/` 는 `late.js` 로 나간다.

**둘로 가른 까닭이 있다.** 판 탭을 여는 날과 회전 탭을 여는 날이 다르다.
한 묶음으로 두면 판 하나 열려고 검사 탭 코드까지 읽는다.

### 옮기면서 세 자리를 고쳤다

| 무엇 | 왜 |
|---|---|
| `fillSel` 셋을 `renderRot` 안으로 | 시작에서 부르던 것이라 조각이 없으면 터진다 |
| `paintKind()` 를 시작에서 뺐다 | 검사 탭 칸만 칠한다. 그 탭을 열 때 칠하면 된다 |
| `$("#kRun").onclick` 을 `checkBind()` 안으로 | 맨 위에서 잇던 것을 함수로 감쌌다 |

**늦게 읽는 조각은 맨 위에서 아무것도 하면 안 된다.**
판 화면 열아홉은 처음부터 그렇게 짰다 (`PLAYREND` 에 자기를 넣는 한 줄뿐이다).
탭 넷은 그렇게 안 짜여 있었고 그래서 옮기면서 고쳤다.

### 천장을 320에서 310으로 내린다

여유가 23KB 면 천장이 아니다. T213 과 T224 에 같은 판단을 했다.

**13KB 를 남긴다.** 남은 판 하나와 F단계 앞머리가 그 안에 든다.
또 닿으면 다음 갚을 자리는 `js/08_script.js` 와 `js/19_library.js` 다.
둘 다 그 탭을 열 때만 쓰는데 지금은 열자마자 읽는다.

### 비워 둔 자리를 채우면서 133px 늘었다 (T322)

`#todaySlots` 는 T166 에 비워 둔 자리다. 로드맵이 무엇이 들어올지도 적어 뒀다.
**나빠진 것이 아니라 예정된 것이 들어온 것이다.**

그래도 그냥 안 올렸다. 처음에는 넉 줄로 짜서 **201px** 이 늘었다.
게이트가 잡았고 줄을 둘로 합쳐 133px 로 줄였다.

    말을 빼지 않고 줄을 합쳤다. **줄일 것은 글자가 아니라 줄이다.**

그리고 하나를 더 찾았다. `check_ui.js` 의 껍데기 950px 한계가 같이 났다.
**세션 중에도 이 칸이 보이고 있었다.** 세션 화면은 블록에 집중하는 자리다.
`body.session-focus #todaySlots{display:none}` 을 붙였다.

게이트 둘이 서로 다른 것을 잡았다. 하나는 **길어진 것**을 잡고
하나는 **안 숨는 것**을 잡았다. 앞엣것은 값을 올려 받아들였고 뒤엣것은 고쳤다.
**올릴 것과 고칠 것을 가르는 것이 게이트를 쓰는 법이다.**

### 넉 턴에 6KB 를 썼다 (T322~T325)

298 에서 304 가 됐다. 연속일과 회복권과 퀘스트가 다 열자마자 읽는 자리다.

| 턴 | 무엇 | KB |
|---|---|---|
| T322 | 연속일 셈과 첫 화면 칸 | 2 |
| T323 | 회복권 셈과 대장 칸 | 2 |
| T324 | 퀘스트가 세는 함수 둘 | 1 |
| T325 | 퀘스트 한 줄과 자료 읽기 | 1 |

**천장까지 6KB 다.** F단계가 T342 까지고 열일곱 턴이 남았다.
그 안에 또 닿는다. 다음 갚을 자리는 `js/08_script.js` 와 `js/19_library.js` 다
(T313 뒤에 적어 뒀다). **T330 앞에서 다시 잰다.**

### 갚았다. 클립 탭 16KB (T331 뒤)

305 에서 288.3KB 가 됐다. 검사는 올림해서 재므로 기준선은 289다. T325 에 "T330 앞에서 다시 잰다" 고 적어 뒀고 그 자리에 왔다.

**갚을 자리를 또 잘못 적어 뒀었다.** T313 뒤에 `08_script.js` 와 `19_library.js` 를
다음 차례로 적었는데 열어 보니 둘 다 **세션이 도는 데 쓰는 조각**이었다.
`renderBlockPane` 이 스물넷 자리에서 부르고 `paintTimer` 가 열다섯 자리에서 불린다.

    큰 것과 늦게 읽어도 되는 것이 다르다.

크기로 고르면 안 된다. **언제 쓰는가로 고른다.**
클립 탭은 가진 파일을 열어 구간을 도는 자리고 날마다 여는 자리가 아니다.

옮기면서 셋을 고쳤다. `mmss` 를 늘 있는 자리로 옮기고(되풀이 목록도 그것을 쓴다),
시작에서 부르던 `renderClip()` 과 `renderScript()` 를 떼고,
자료 탭이 클립으로 보낼 때 먼저 그리던 줄을 뗐다.

**늦게 읽는 조각은 맨 위에서 아무것도 하면 안 된다.** T313 뒤에 적은 그대로다.
두 번째로 옮기니 그 규칙이 먼저 떠올랐고 옮기기 전에 세 자리를 찾아 뒀다.

천장을 310 에서 300 으로 내린다. 여유 12KB 다.
다음 갚을 자리는 **되풀이(`16_review.js` 5KB)와 소리 설정(`17_sound.js` 6.6KB)** 이다.
둘 다 탭을 열 때만 쓴다. 다만 소리는 시작에서 `renderSound()` 를 부르므로
그 줄을 같이 봐야 한다.


### 갚았다. 자료 탭과 규칙 탭 15KB (T361)

296 에서 280.6KB 가 됐다. 검사는 올림해서 재므로 기준선은 281 이다.
T359 에 **천장까지 4KB** 라고 적어 뒀고 그 자리에 왔다.

고르는 법은 T331 뒤에 정한 그대로다. **크기가 아니라 언제 쓰는가로 고른다.**
자료 탭은 어디서 온 자료인지를 보는 자리고 규칙 탭은 판 규칙을 읽는 자리다.
T335 에 **"규칙 탭은 아무도 안 연다"** 고 적어 뒀다. 그 줄이 이 턴의 근거다.

`SOURCES` 와 `SRC_COND` 와 `renderDocs`/`renderSrc`/`renderRules` 를
`app/late/23_docs.js` 로 옮겼다. `app/js/20_docs.js` 에는 시작 부트스트랩만 남았다.

#### 규칙 탭이 시작할 때만 그려지고 있었다

옮기다 보니 `renderRules()` 가 **시작에서 한 번 불리고 탭을 열 때는 안 불리고**
있었다. 첫 그림에 값을 다 넣어 두고 그 뒤로는 안 고치는 꼴이었다.

    if(name==="rules"){ lateDo("renderRules"); renderSplit(); }

늦게 읽는 자리로 오면서 그것이 저절로 맞았다. 탭을 열 때 그리는 것이 옳다.
**늦게 읽는 조각은 맨 위에서 아무것도 하면 안 된다**는 규칙(T313 뒤)이
없던 문제까지 같이 고친 자리다.

#### 자료 탭이 그려지는지를 아무도 안 재고 있었다

옮기고 나서 **무엇이 이 옮김을 지키는가**를 찾아봤다. 없었다.

    scripts/ 어디에도 `srcList` 도 `renderSrc` 도 안 나온다.

탭을 여는 검사는 있다. `check_contrast.js` 와 `check_friction.js` 가 탭 열둘을
차례로 연다. 그런데 그것들은 **열린 뒤에 있는 것**을 잰다. 빈 탭도 대비는 통과다.

늦게 읽는 조각은 다른 조각과 다르게 틀린다. **파일은 멀쩡하고 화면만 빈다.**
그래서 이 턴에 `scripts/check_late.js` 를 만들었다. 늦게 읽는 자리 아홉마다
다섯을 잰다. 조각에 있는가, `lateDo` 로 부르는가, 바로 부르지 않는가,
열기 전엔 비었는가, **열면 채워지는가.**

`app/order.txt` 의 `late/` 조각을 세서 표가 다 덮는지도 본다.
아홉째 조각을 늘리고 검사를 안 늘리면 거기서 걸린다.

깸 셋을 넣어 셋 다 잡혔다.

| 깸 | 잡은 것 |
|---|---|
| `lateDo("renderSrc")` 를 뺐다 | 부르는 자리가 없다 + 탭이 비었다 (2판) |
| 시작에서 `renderRules()` 를 부른다 | 바로 부른다 + 화면 오류 2개 (2판) |
| 표에서 `22_track.js` 줄을 뺐다 | 조각을 안 본다 (1판) |

#### 적어 둔 다음 자리가 또 틀렸다

T331 뒤에 **"다음 갚을 자리는 되풀이(`16_review.js`)와 소리 설정(`17_sound.js`)"**
이라고 적어 뒀다. 열어 보니 둘 다 못 옮긴다.

| 조각 | 왜 못 옮기나 |
|---|---|
| `17_sound.js` | `spkBtn` 을 오늘 탭·미디어·판정·클립이 쓴다. 탭 하나 것이 아니다 |
| `16_review.js` | `renderNudge()` 를 오늘 탭이 그릴 때마다 부른다 |

T313 뒤에 적은 다음 자리도 틀렸었고(둘 다 세션이 도는 조각이었다) 이번이 두 번째다.
**적어 둘 때는 크기만 보고 적고 옮길 때는 쓰이는 자리를 본다.** 그래서 어긋난다.
다음 자리를 적을 때는 부르는 자리를 세어 보고 적는다.

#### 다음 갚을 자리

`22_paircode.js`(12KB)와 `23_merge.js`(23KB)다. 둘 다 **대장 탭에서만** 부른다
(`renderPair`/`renderMerge`, `04_today.js` 의 대장 갈래 한 줄). 합해서 35KB 다.

부르는 자리를 세어 봤다. 대장 탭 밖에서 부르는 곳은 `11_ledger.js` 의 파일 받는
자리 하나뿐이고 그것도 같은 탭 안이다. 다만 **검사 셋이 `mergePlan` 을 바로 부른다**
(`check_store`, `check_adapt`, `check_ui`). 옮기면 그 검사들이 먼저 `lateDo` 를
불러야 한다. 그 값이 이 옮김의 진짜 비용이다.

여유가 19KB 라 급하지 않다. **천장까지 5KB 남을 때 다시 잰다.**
