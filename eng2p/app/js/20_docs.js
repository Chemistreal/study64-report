/* =========================================================================
   시작. **여기가 앱이 처음 도는 자리다.**

   자료 탭과 규칙 탭 그리기는 `late/23_docs.js` 로 갔다 (T361).
   둘 다 거의 안 여는 자리라 열자마자 읽을 까닭이 없다.
   ========================================================================= */
buildNav();
/* **회전 탭 고르개와 검사 탭 칸은 여기서 안 채운다** (T313 뒤).
   그 코드가 늦게 오는 자리로 갔다. 채우는 일은 그 탭을 열 때 그쪽이 한다. */
/* 끊긴 세션이 있으면 그 자리에서 다시 편다. 저절로 안 돈다. 누르면 이어 간다. */
loadSession();
/* **못 읽은 기록이 있으면 제일 먼저 말한다.** 조용히 넘어가면 두 사람이
   빈 화면을 보고 다시 시작하고 그 다음 저장이 옛 기록 위를 덮는다. */
if(typeof LOAD_ERR!=="undefined" && LOAD_ERR) toast(LOAD_ERR);
/* **오늘 그 한 주를 먼저 읽고 그린다.** 48주 내용이 분기 조각 넷에 있고
   첫 그림에 쓰는 것은 오늘 주뿐이다 (T245).
   주차는 `plan()` 이 `S.days` 로 세므로 차림표 없이도 안다. 고리가 없다.
   못 읽어도 그린다. **안 그리면 화면이 빈 채로 남는다.** 그때는 오늘 한 장이
   차림표를 못 읽었다고 말한다. 그 말은 이미 있다. */
applyFs(); renderOnboard();
needWeek(plan().week, function(){
  /* 규칙 탭은 그 탭을 열 때 그린다 (T361). 늦게 읽는 조각이라
     시작에는 그 함수가 없다. **늦게 읽는 것을 시작에서 부르면 안 된다.** */
  renderToday(); paintTimer(); paintSide(); paintVeil();
});
/* 클립과 대본은 그 탭을 열 때 그린다 (T331 뒤). 늦게 읽는 조각이라
   시작에는 그 함수가 없다. **늦게 읽는 것을 시작에서 부르면 안 된다.** */
ttsVoices(); renderSound();
/* **안 되는 기기에서는 안 된다고 말한다** (T381). 진동 칸이 눌러도 아무 일이
   없으면 두 사람은 앱이 고장 난 줄 안다. 첫 화면에 있는 칸이라 여기서 그린다. */
paintBuzz();
/* **끊긴 세션이 있으면 오늘 탭으로 데려온다.** T247

   블록 1은 미디어 탭에서 40분을 듣는다. 그 사이에 기기가 꺼지면 다시 열 때
   주소의 `#media` 가 복원되고 미디어 탭이 열린다.
   그런데 **이어서 칸은 오늘 탭에 있다.** 그러면 끊긴 것을 아무도 못 본다.
   조작줄에 오늘 단추가 있어 갈 수는 있지만 (T182) 가야 하는 줄을 모른다.

   오늘 것이 시작됐고 아직 안 끝났으면 그 칸을 먼저 보인다. */
(function(){
  var want=location.hash.slice(1)||"today";
  var s=S.session, mid=(s && s.date===today() && (s.idx>0 || s.left<BLOCKS[0].m*60));
  go(mid ? "today" : want);
})();
window.addEventListener("hashchange",function(){ go(location.hash.slice(1)||"today"); });
